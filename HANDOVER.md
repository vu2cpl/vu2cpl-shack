# Session Handover — VU2CPL Shack

**Period:** 2026-05-01 → 2026-07-01
**Operator:** Manoj VU2CPL · MK83TE · Bengaluru
**Last commit at handover:** repo-tooling doc pass — caught up `SHACK_CHANGELOG.pdf` (was 12 commits stale) and added CLAUDE.md rule #6 to keep it paired with `SHACK_CHANGELOG.md` going forward (mirrors rule #3's DXCC.md pairing). Immediately prior: `5acf437` retired the `clublog_dxcc_tracker_v7.json` DXCC-tab extract + CLAUDE.md rule #4 (kept its number; retired in place) + simplified `nrsave` (landed as two commits, `84ad7f0`+`5acf437`, after a `git add -u` pathspec error split what was meant to be one); the Pi's live `~/.bashrc` `nrsave` — which Claude Code can't reach directly — was hand-fixed by the operator same-day, verified matching. Same-day chain from `5c708ed`: new UberSDR dashboard → `346e552` decoder-mode split (v17) → `419aa28` waterfall-listener headline (v18) → `590e06b` by-band bar scoped to waterfall (v19) → extract retirement → changelog-PDF rule. Prior tip `ea70bf0` = SPE power meter RAW/AVG/PEAK, build v15. (rotator-remote web UI = `vu2cpl/rotator-remote` `7fc5411`.)

---

## Repo state

```
~/projects/vu2cpl-shack    main   clean, in sync with origin
```

`.DS_Store` is the only untracked thing — ignore.

---

## What landed this week

| Day | Area | What changed |
|-----|------|--------------|
| 07-01 | Repo tooling — SHACK_CHANGELOG.pdf caught up + new rule #6 | Found 6.7 MB `SHACK_CHANGELOG.pdf` **12 commits stale** (last regen 2026-06-12, `9f11bb6`) — no rule enforced it, unlike the DXCC.md/PDF pairing (rule #3), so it had quietly drifted. Regenerated (`npx --yes md-to-pdf`) and added **CLAUDE.md rule #6** mirroring rule #3 so it can't lapse again. See SHACK_CHANGELOG 2026-07-01. |
| 07-01 | Repo tooling — retired DXCC-extract file | `clublog_dxcc_tracker_v7.json` (a filtered copy of the DXCC tab's nodes out of `flows.json`, a fossil from when DXCC Tracker was its own standalone flow pre-merge) removed via `git rm` — it carried no info not already in `flows.json` and existed only to be kept in sync on every commit. CLAUDE.md rule #4 retired **in place** (number kept — 11 historical entries reference "rule #4"; renumbering would've silently repointed them at rule #5). `nrsave` simplified in both `REBUILD_PI.md` and `rebuild_pi.sh` to just stage+commit `flows.json`. Also cleaned: `DXCC.md` (files table, commit sequence, a stray "Flow file" mislabel), `README.md` repo-layout tree, `HANDOVER.md` "Key files" list. `DXCC_Tracker_README.pdf` regenerated per rule #3 (DXCC.md changed). **Resolved same day:** the live `~/.bashrc` `nrsave` on the Pi (which Claude Code can't reach directly) was hand-swapped by the operator — backed up first, byte-exact Python replace, verified via `sed`; now matches the simplified stage+commit-only body. See SHACK_CHANGELOG 2026-07-01. |
| 07-01 | UberSDR receiver monitor — new card, both dashboards | New `UberSDR` flow tab (`ubersdr_tab`, 6 nodes) subscribes to `ubersdr/metrics/sessions` + `ubersdr/metrics/voice_activity/#` (12 bands) on the shack broker. `ubersdr_agg` categorises sessions (real listeners vs decoders vs spectrum/noise-floor monitors), buckets listeners by band (with an `Other` catch-all for out-of-band tuning — the biggest bucket), sums per-thread CPU + total kbps→Mbps, and merges per-band noise-floor + voice-activity into one flat payload feeding **both** D1 `ubersdr_panel` (Shack Monitoring tools tab) and Vue `UberSdrCard` (topic `ubersdr` via `ubersdr_vue_bridge`→`uib_shack_01`; `CARDS.ubersdr`, build `v16`, cache-buster `?v=16`). Read-only consumer. Aggregator was verified against live captured `sessions` + all 12 `voice_activity` payloads before any UI was wired. **Same-day enhancement (build v17):** `ubersdr_agg` now emits `decodersByMode` (FT8/FT4/CW/WSPR counts, parsed from the `decoder-<band>-<mode>` channel string) + a `viewers` count (real users watching the waterfall, i.e. `is_internal=false && is_spectrum=true`); both cards render a "Decoders by mode" line. **Build v18 (operator steer):** the *waterfall* session (`is_spectrum && !is_internal`) is the actual human listener — audio/IQ streams are automated — so both cards now headline the **waterfall count** as the big bold "Listeners (waterfall)" number and demote the total/IQ counts to small sub-text; bar list relabeled "Sessions by band" (temporary). **Build v19 (same steer):** the by-band bar graph still bucketed all real-user sessions, not just waterfall — fixed `ubersdr_agg`'s `byBand` to only count `is_spectrum && !is_internal` sessions (verified live: drops to the single actual 40m listener), and relabeled both cards back to "Listeners by band (waterfall)". See SHACK_CHANGELOG 2026-07-01. |
| 06-27 | SPE power meter — RAW/AVG/PEAK in both dashboards | `spe-remote` `serial_handler.py` derives `p_out_avg` (EMA, α=0.15) + `p_out_peak` (peak-hold, constant 600 W/s decay) on `AmplifierState`, both reset across op/tx transitions; `protocol.py` carries them (server commit `f5537ec` on `spe-remote` main). `ws_format_state` maps them to `pwr_avg`/`pwr_peak` (**null-on-absent**); the one flat payload feeds both UIs. D1 `ws_panel_node` got a RAW/AVG/PEAK pill group + `speRenderPwr()` (fallback-to-raw, caches last payload so a pill click re-renders without a new msg, `window.speSetMode()` global). Vue `/shack` SPECard got `.pill-group` pills + a `pwrDisplay` computed every meter element reads from + `pwrHasAvg`/`pwrHasPeak` gating; build `v15`, cache-buster `?v=15`. Mode persists in `localStorage` `speMeterMode`, **shared across `/ui`+`/shack`** (same origin). Landed from a contributed package (adersh fork) whose peak decay compounded `decay_s` per frame (accelerating, sample-rate-dependent, 1500→0 in ~0.46 s) — replaced with a per-frame `dt`-scaled decrement (verified 2.50 s constant). PEAK is a *sampled* 25 Hz peak, not true envelope PEP. Every layer degrades gracefully to RAW (deploy in any order). See SHACK_CHANGELOG 2026-06-27. |
| 06-19 | SPE band-sweep landed across all UIs | spe-remote Phase 2 work (orchestrated TUNE + ATU band sweep via Flex 6000 over SmartSDR TCP, with the SPE manual's 154 sub-band centers, in-band filter, 60m override to 5.357 MHz, VFO save/restore) is now reachable from four control surfaces sharing one `tune_event` broadcast: (1) MacExpert SwiftUI app's new SWEEP modal (macexpert-spe `f85df7d`); (2) spe-remote's bundled web dashboard SWEEP button + inline panel; (3) Node-RED `/ui` SPE Panel — patched `ws_btn_router` for `tune_*` pass-through, `ws_parse_node` for a 4th output routing `tune_event` on topic `spe/tune`, `ws_panel_node` HTML+JS for the panel (commits `9eb615f` first cut, `b4c8c11` fixing the scope binding); (4) Vue `/shack` SPECard — added a 4th button on the controls row plus a `v-if`-toggled sweep panel reading `tune_event` via `uibuilder.onTopic('spe/tune')`, command path through `vue_spe_cmd_router_01` (commit `945f96c`). Plus Flex UDP discovery on port 4992 so `flex.host` can be left empty in `config.yaml`. Rebuild_pi.sh Stage 13c also prompts for `flex.enabled` + host when the inventory has both `HW_SPE=1` and `HW_FLEX=1`. Two stale code comments revealed by the work and fixed: WebSocket dispatchers (both web dashboard and Node-RED parse) now only treat a JSON as state when `op_status` is present — closes the same misdecode-as-defaulted-state class of bug that `d3be36f` closed on MacExpert and `26f2653` closed in spe-remote's parser. |
| 05-01 | AS3935 daemon | Revived after silent 10-day outage. Hardened `as3935_mqtt.py`: MQTT retry, LWT, 30 s heartbeat, I²C self-test, clean SIGTERM, indoor antenna mode (AFE_GB=0x12) |
| 05-01 | Open-Meteo | `lightning_potential` is null in IN. Switched to `cape` + `weather_code`, mapping rewritten (LPI in J/kg ≠ 0–100 %). Output 1 → strike chain, output 2 reserved for status |
| 05-01 | Strike labels | Source field now propagates Parse Strike → Trigger Disconnect; "Blitzortung" default removed (Blitzortung TCP not wired anyway) |
| 05-01 | Dashboard | `flow.strikes` + `flow.event_log` now persist server-side; replayed via existing 30 s `Refresh Stats` tick. Page refresh no longer wipes log/map |
| 05-06 | AS3935 IRQ | **GPIO17 in software, GPIO4 in hardware** — discovered by SRCO scan. Counter went from 0 to working immediately after IRQ_PIN=4 |
| 05-06 | LC tank | New helper `as3935_tune.py` sweeps TUN_CAP 0–15. Tuned to **TUN_CAP=10 → 499.9 kHz, -0.02 %** |
| 05-06 | Chip init | Added `CALIB_RCO` at startup, INT register flush after config writes, retained status payload includes `tun_cap`/`irq_pin`/`calib_*` |
| 05-06 | Dashboard | AS3935 panel shows live `✓ READY · NF=4 · up Nm · irq=N`. Two new MQTT-in nodes + `Format AS3935 State` + `Replay AS3935 State`. Event Log moved above Map. |
| 05-07 | Folders | Moved `~/Documents/vu2cpl website/` → `~/projects/vu2cpl-website/` and `~/vu2cpl.github.io/` → `~/projects/vu2cpl.github.io/`. CLAUDE.md path refs updated |
| 05-08 | Lightning Detect map | Ripped out entirely. Both active strike sources (Open-Meteo, AS3935) only know distance — every dot stacked on the home marker, so the map showed nothing useful. Master Dashboard ui_template trimmed by ~100 lines (HTML + CSS + Leaflet imports + `initMap()` + strikeLayer/lzCnt code in 4 message handlers). Payload `lat/lon` fields kept for future Blitzortung wiring |
| 05-08 | Nearest Strike gauge | Now persists across page refresh. `Strike → Dashboard` writes `flow.last_strike_km`; `Replay on lightning tab` emits `{type:'strikes_replay', lastKm}` on the 30 s tick; dashboard handler calls `drawGauge(lastKm)`. Boot-time `drawGauge(200)` dropped — gauge starts at "—" until first strike. **Gauge later removed entirely** when the Lightning tab was merged into the Shack tab (see below). |
| 05-08 | Lightning UI → Shack tab | Lightning Detect dashboard tab deleted. Master Dashboard moved to a new `Lightning Protection` group on Shack tab (width 12, order 9). Internal header card + weather card removed (Shack tab supplies them). Nearest Strike gauge removed. Reconnect ↺ buttons grew labels (`↺ RECONNECT`) and match switch height. |
| 05-08 | Bypass switch | New vertical `BYPASS` button between ANTENNA and RADIO. 120-min countdown, auto-expires, never survives Node-RED restart. While ON: yellow banner + amber strike alerts; Trigger Disconnect early-outs (no MQTT off, no reconnect timer). Activation force-reconnects ant + radio. New nodes: http-in `/lightning/bypass` + `Bypass Handler` function (3 outputs) + http response. `flow.bypass_active` / `flow.bypass_expires_at` reset by Init Defaults; replayed every 30 s by `Replay on lightning tab`. Verified end-to-end during a moderate-CAPE day. |
| 05-08 | Last-activity recap | Old `✔ No recent activity` filler text replaced. Boot: `⏱ Awaiting first event`. After 30 s of no new alert: muted `⏱ Last: <previous text> · Nm ago` with auto-refreshing relative time. Banner is always visible — no more lying or auto-hide. |
| 05-09 | LP-700 → WS gateway | [VU3ESV/LP-700-Server](https://github.com/VU3ESV/LP-700-Server) running as `lp700-server.service` on Pi @ `:8089`. Node-RED LP-700 tab migrated from direct `@gdziuba/node-red-usbhid` to a websocket-client of the gateway. Tab renamed `LP-700-HID ws`, trimmed 25→18 nodes. `LP State Aggregator` + `LP-700 Panel` + button router kept; `LP Dice and Slice`, `Poll Meter Values`, HID config, all polling injects + raw-buffer debug deleted. Buttons now emit JSON `{type:'command', action:'channel_step'\|'range_step'}` to ws-out. The 7 verb-test inject buttons from VU3ESV's example flow kept on canvas as testing affordances. Multi-client unlocked — Mac SwiftUI app (TODO #12) can subscribe in parallel. Post-deploy: aggregator was reading `msg.power_avg` (legacy KD4Z shape) instead of `msg.payload.power_avg` (Reshape output) → dashboard stuck at last cached values; fixed in 08c907f. |
| 05-09 | LICENSE | Added MIT LICENSE + README badge. Third-party note covers VU3ESV gateway nodes embedded in flows.json (upstream unlicensed, retained terms). |
| 05-09 | Pi-side scripts checked in | `rpi_agent.py` (HTTP reboot/shutdown agent), `monitor.sh` (MQTT telemetry cron), `rpi-agent.service` (systemd unit). Discovered during a CLAUDE.md audit that the agent doc was overstating its job — it's HTTP-only; telemetry comes from `monitor.sh` via per-minute crontab. CLAUDE.md split into separate "HTTP control agent" + "Telemetry publisher" subsections with copy/paste install commands. Backup section + new "Pi-side scripts in this repo" table added. |
| 05-09 | More Pi-side scripts in | `power_spe_on.py` (SPE Expert 1.5 KFA power-on FTDI helper) added to repo. `enable_file_context.sh` was already tracked (predated my audit). `fetch_clublog.sh` deleted from the Pi entirely — it was an old artifact whose `nr_dxcc_live.json` output is referenced by zero nodes in flows.json (verified). Live DXCC fetch happens in-flow via `Build Club Log API Request` → `Fetch All Modes + Parse` → `nr_dxcc_seed.json`. Club Log password + API key rotated since they had been pasted in plaintext. |
| 05-09 | `DEPLOY_PI.md` runbook | New top-level doc walking through full per-Pi onboarding: scp files, ownership fix, mosquitto-clients install, cron, sudoers, systemd, Node-RED httpDevices update, dashboard verify. Also covers HA Pi special case, 8-row troubleshooting table, and clean decommission flow. CLAUDE.md cross-references it. Use this when finally bringing the 2 remaining Pis online (HANDOVER follow-up #4). |
| 05-09 | Pi GPS NTP server | New standalone repo [vu2cpl/pi-gps-ntp-server](https://github.com/vu2cpl/pi-gps-ntp-server). Project pivoted from the originally-planned ESP32 firmware build (rationale: chrony + gpsd + kernel PPS on a Pi gives more accurate client-visible time, with orders of magnitude less custom code, than ESP32 firmware). Pi 3B repurposed as `gpsntp.local` @ 192.168.1.158. Taps the U3S's QLG1 GPS via the unused **6-way** header (2.2 kΩ + 3.3 kΩ voltage dividers — QLG1 outputs 5 V, Pi GPIO is 3.3 V only); U3S still has its own QLG1 connection on the 4-way and is electrically undisturbed. Stack: Pi OS Lite 64-bit Trixie, `gpsd 3.25` (`-n` flag), kernel PPS via `dtoverlay=pps-gpio,gpiopin=18` → `/dev/pps0`, chrony with `refclock PPS /dev/pps0 lock NMEA refid PPS` + `allow 192.168.1.0/24` + `allow fd00::/8` (IPv6 ULA). Headline numbers at first lock: stratum 1, PPS error ±152 ns, system clock 35 ns slow of GPS, skew 0.009 ppm, root dispersion 18 µs. Mac disciplined via `sudo systemsetup -setnetworktimeserver gpsntp.local`; `timed` keeps it within 1–4 ms over LAN. Repo carries MIT LICENSE + README + BUILD.md (stage-by-stage procedure with per-stage verification + troubleshooting) + HANDOVER.md (context, decisions, ops checks). Local dir `~/projects/Pi GPS NTP Server/` (renamed from `ESP32 GPS NTP/`). |
| 05-09 | gpsntp on RPi Fleet | First Pi onboarded via the brand-new `DEPLOY_PI.md` runbook (above). `rpi_agent.py` + `monitor.sh` + `rpi-agent.service` deployed; sudoers `vu2cpl ALL=(ALL) NOPASSWD: /sbin/reboot, /sbin/shutdown` added; `mosquitto-clients` installed; cron publishes 7 MQTT topics per minute (`cpu/mem/temp/disk/uptime/ip/status` under `rpi/gpsntp/`) to broker @ 192.168.1.169. Smoke test caught all 7 messages cleanly with `mosquitto_sub -C 7` (DEPLOY_PI.md updated implicitly: `monitor.sh` publishes without `-r`, so sub must subscribe before pub). `rpi-agent.service` active on :7799, HTTP 404 probe verified. Node-RED `httpDevices` map (function `a0695975fec84e2c`) still needs the `'gpsntp':'http://gpsntp.local:7799'` row — flagged as follow-up #9. |
| 05-10 | Lightning tier-1 cleanup | Audit pass on Lightning Antenna Protector tab dropped 2 dead nodes (`Save Reconnect`, `UI Stats`), 2 dead wires (Parse Weather → Master Dashboard, AS3935-within-threshold → Send Radio Command), and 5 dead `flow.set` lines (`map_count`, `connectCount`, `wssStatus`). 80 → 78 nodes. Operator also reduced Parse Weather output count 2 → 1 (cleaner than the wire-only fix). |
| 05-10 | Lightning tier-2 cleanup | Two passthrough/indirection nodes deleted: `Check Threshold` (computed `msg.shouldDisconnect` and `msg.thresholdKm` — neither field read by any downstream node) and `Refresh Stats` (1-line `return msg` fan-out). Haversine now wires directly to `Within threshold?`; `Stats refresh every 30s` inject fans out to its 5 destinations directly. 78 → 76 nodes. |
| 05-10 | Lightning tier-3 cleanup | Renamed `Replay on lightning tab` → `Replay Bypass State` (post-Shack-tab the old name was misleading). `Init Defaults` `node.warn` line deleted (status badge was already showing the same info). `Strike → Dashboard` and `AS3935 → Dashboard` merged into a single function detecting AS3935 by `msg.strike.source`. AS3935 Threshold Check now feeds the unified function directly (before the threshold switch) so distant AS3935 strikes banner-render with amber/green colour, matching the OM path. `Within threshold?` switch (OM path) trimmed from 2 rules/outputs to 1 — its `else` branch was always empty. 76 → 75 nodes. Audit complete: 80 → 75 over three tiers. |
| 05-10 | Docs reorg | `README.md` was the DXCC reference doc with a station footer — predated every other subsystem write-up. Split into umbrella `README.md` (hardware + 11-subsystem summary + repo layout + doc map, ~290 lines) plus dedicated `DXCC.md` (previous content verbatim, retitled). `DXCC_Tracker_README.pdf` now regenerates from `DXCC.md`. CLAUDE.md rule #3 + the SHACK_CHANGELOG header updated to reference the new pairing. Future per-subsystem docs (e.g. `LIGHTNING.md`) can now slot in cleanly without bloating the GitHub front page. |
| 05-10 | `REBUILD_PI.md` + `as3935.service` | Disaster-recovery gap closed. `as3935.service` systemd unit (previously only on the Pi, not in the repo) added alongside the existing `rpi-agent.service`. New top-level `REBUILD_PI.md` runbook walks through full from-scratch rebuild of the main shack Pi in 12 steps (OS install → mosquitto → Node-RED + palette → Projects feature → GitHub SSH + clone → file context store → Pi-side scripts + systemd + sudoers + cron → hardware/udev → lp700-server → DXCC creds → Tasmota broker check → 12-point verification + 6-row failure-modes table). README + CLAUDE.md cross-link it. README's doc-map now distinguishes `REBUILD_PI.md` (this Pi, full install) from `DEPLOY_PI.md` (a *different* Pi as a fleet member). |
| 05-10 | DXCC: VU2OY IP→DNS | Two stale doc references (DXCC.md cluster table + CLAUDE.md DXCC bullet) still cited the VU2OY cluster by IP `103.153.92.118`. flows.json was already exclusively using `vu2oy.ddns.net`. Both docs updated; DXCC PDF regenerated. |
| 05-10 | DXCC: N2WQ disconnect cycle fix | N2WQ was kicking us in a tight loop. Two interlocking causes. (1) Login regex `includes('login:')` matched the welcome banner's `Last login:` line, triggering a duplicate `VU2CPL\r\n` send → AR-Cluster's no-duplicates policy kicked us. Fixed by tightening to `length < 40 && endsWith('login:'/'callsign:'/etc.)`. (2) Even after the regex fix, kicks continued because **another LAN device was also logged in as VU2CPL** (never identified — not Mac, not Pi; possibly iOS app, logging program, or forgotten `nc` session). Sidestepped via packet-radio SSID: Credentials node now has `cl_login_ssid: '-1'`; `Login + Parse + Dedup` sends `(callsign + ssid)`. Node-RED now logs in as `VU2CPL-1`, coexists with the unknown `VU2CPL` ghost. Verified `VU2CPL-1` accepted by N2WQ-2 via manual `nc` test pre-deploy. |
| 05-10 | DXCC: secrets → systemd Environment= | While Credentials node was being touched, API key, password, Telegram token moved out of flow.func and into `/etc/systemd/system/nodered.service.d/secrets.conf` (chmod 600, root-owned). Node reads each via `env.get('VAR')` with pre-flight validation that fires `node.error` + red status badge if any var is missing. Motivation: not repo exposure (private) — rotation friction. Now: edit secrets.conf + `systemctl restart nodered`. No commit, no Deploy. `cl_email`/`cl_callsign`/`cl_login_ssid`/`tg_chat_id` stay inline (not secrets). |
| 05-10 | DXCC: Bootstrap resilience (corrected) | The post-secrets redeploy paused the tracker. Initial diagnosis identified two issues: (1) `Load Club Log on startup` + `Retry Club Log (90s)` had `once: false` ignoring their `onceDelay`, so neither fired on deploy; and (2) `dxccReady` was only set by successful Club Log fetch, leaving cached-data-only operation impossible. Fix #2 was correct: `Bootstrap Worked Table` now flips `dxccReady=true` in all three success branches. Fix #1 was **wrong and reverted**: those injects are deliberately `once: false` as an **anti-ban measure** — Club Log had previously rate-limited the API key, and the design is 1 API call/day via 02:00 cron only. The Bootstrap fix alone is sufficient (tracker works from cached `nr_dxcc_seed.json` with zero API calls on restart). Lesson: CLAUDE.md TODO #10 ("Club Log API ban status") was the trail that should have prevented the misdiagnosis. |
| 05-10 | DXCC: real regression — spot field-name mismatch | After all the above, alerts STILL weren't firing despite all 4 clusters green and spots flowing. Tracked down to a long-standing field-name mismatch between `Login + Parse + Dedup` (emitting `spot.dxCall`, no `spot.band`) and `DXCC Prefix Lookup + Alert Classify` (reading `spot.call` and `spot.band`). The lookup hit `if (!band) return null` on every real spot and returned silently — empty node status, no alerts. TEST inject buttons set `call`/`band` directly in their payloads so manual tests worked, masking the bug. Fix: added `getBand(khz)` helper to `Login + Parse + Dedup` and emit `call` (alongside `dxCall` for backwards compat) and `band` in the spot object. Diagnosis lesson: when a downstream node shows empty status while upstream is active, suspect a field-name mismatch before chasing flags/state. |
| 05-10 | Chrony / GPS Time Server card | New dashboard for `gpsntp.local` (the stratum-1 GPS-disciplined NTP server installed 2026-05-09). `743a0d8` pushed a transitional 7-widget version on a dedicated `GPS NTP` flow tab; `286348e` migrated to the target single-`ui_template` design from [`pi-gps-ntp-server`](https://github.com/vu2cpl/pi-gps-ntp-server) `dashboard/`. Final placement: flow tab `GPS NTP (card)` (id `4590ed80de4873b1`) with just `mqtt in shack/gpsntp/chrony` + `Chrony status card` ui_template (id `38e130c3`); dashboard tab `Shack Monitoring tools` → group `Network Monitor` (width 6). Topic retained JSON, cron-published once/min by `/usr/local/bin/gpsntp-mqtt-publish.sh` (Pi-side, not in this repo). Threshold-aware (orange when offset/dispersion/skew exceeds tolerance, ref source ≠ PPS, fix < 3D). The empty `GPS NTP` flow tab (`4cac0c07`) was deleted afterwards. |
| 05-10 | `rebuild_pi.sh` automation | New 619-line bash script automating Stages 2–13 of REBUILD_PI.md. Stage-based, idempotent, resumable via `/tmp/rebuild_pi.state`, fail-fast with `set -euo pipefail`. Two unavoidable interactive pauses: Stage 6 (paste new SSH key into GitHub) and Stage 12 (`read -s` Club Log + Telegram secrets, written to systemd drop-in). Built-in 10-point verification at Stage 13. Wall-clock ~30 min vs ~90 min manual. REBUILD_PI.md updated to point at the script as the faster path; runbook stays as manual fallback + source-of-truth for what each stage does. Both must stay in sync (script banners reference runbook section numbers). |
| 05-13 | Lightning audit — fix AS3935 disconnect regression + 10 cleanups | Audit pass on Lightning Antenna Protector tab found a **critical regression** silently introduced by yesterday's `76d60e5` (distance-graded disconnect). The new source filter used strict equality `source !== 'AS3935'` but `AS3935 Threshold Check` sets `msg.strike.source = 'AS3935 (local)'` — so real AS3935 lightning events were early-rejected by `Trigger Disconnect`. Event log + JSONL kept writing `"DISCONNECT triggered"` via the parallel `AS3935 Disconnect Log` wire while no MQTT was actually published to the antenna switch. Dashboard lied; antenna stayed connected. **Fix:** filter now rejects only `Open-Meteo`; AS3935 / TEST / any other source falls through the matrix. Only sources containing `AS3935` populate the corroboration window (`recent_as3935`) — TEST injects exercise the matrix without polluting that counter. Same commit bundled ten follow-up issues: (1) dead `Within threshold?` switch (`12c029b5...`) deleted — the matrix's own zone gate makes it redundant and a lower-than-medium slider value would have hidden valid strikes from the matrix; (2) `AS3935 Disconnect Log` now bypass-aware, writes `BYPASS · disconnect suppressed` + `event_record.type='disconnect_suppressed'` when bypass active; (3) `Manual Override`, `Force Reconnect`, `HTTP → Antenna ON` each gained an output wired to `Reconnect Timer` with `payload:'cancel'` so a pending strike-triggered timer can't override a manual action later; (4) three stray `node.warn` debug lines stripped (`Stats → Dashboard` 5-line block firing every 30 s, `Reconnect Timer` FIRED warn, `Bypass Handler` action warn); (5) unused `flow.recent` push dropped from Haversine; (6) stale comment in `Parse AS3935` fixed (63 not 40 is the out-of-range sentinel; code was always right); (7) `Replay AS3935 State` uses explicit `node.send` loop instead of `return [out]` fan-out form. Lightning tab 76 → 75 nodes. CLAUDE.md "Lightning Antenna Protector" section updated for source-filter wording; node-count cell corrected 71 → 76 (stale since 05-08). SHACK_CHANGELOG.md 2026-05-13 has the full account incl. diagnosis lesson on parallel log wires. Commit [`f5f65b2`](https://github.com/vu2cpl/vu2cpl-shack/commit/f5f65b2). |
| 05-13 | Lightning dashboard — ALL CLEAR boot state | Operator preference: post-relaunch / no-events-yet state should read as reassuring, not pending. `Master Dashboard` ui_template (`557083037f168b22`) used to render `⏱ Awaiting first event` in muted grey on first paint and after the 30-second event-recap timeout when no prior alert existed. Now renders `✔ ALL CLEAR` in green (`#3fb950`) — same styling as the existing `> 50 km` strike path. Two surgical changes inside the `format` string: (a) initial HTML — `<div id="alertBox">` ships with inline `background:#0d2818; border-color:#238636;` + `✔` icon + green `ALL CLEAR` text; (b) `clearAlert()` `if (!lastAlert)` branch — `paintAlert('✔','ALL CLEAR','#0d2818','#238636','#3fb950')` instead of `paintAlert('⏱','Awaiting first event','#161b22','#21262d','#8b949e')`. Post-first-event recap path (`Last: <text> · Nm ago`) untouched — kicks in 30 s after every real strike, ages via 30 s refresh. Commit [`0a8f69c`](https://github.com/vu2cpl/vu2cpl-shack/commit/0a8f69c). |
| 05-22 | Lightning event log documentation | **Documentation audit:** JSONL event log file (`nr_lightning_events.jsonl`) and its hardcoded config path (`cfg_events_jsonl` in **Init Defaults** node `ec1fd4dece8c4dc0`) were previously undocumented in operator-facing guides. README.md mentioned Lightning subsystem but not event persistence; REBUILD_PI.md verified auto-disconnect worked but didn't check that the JSONL file existed. New operator would have no idea where strike events were logged. **Fixes:** (1) README.md Lightning section — added "Event logging" paragraph explaining JSONL persistence, path config in Init Defaults, restart durability. (2) REBUILD_PI.md final verification checklist — added check #13 (JSONL file exists and contains recent events); upgraded from 13-point to 14-point checklist; added failure-mode guidance for check #13 + updated Chrony failure reference from #13 to #14. New operator following REBUILD_PI.md will now discover the event log location and know how to verify it. Commit [`f08cc9d`](https://github.com/vu2cpl/vu2cpl-shack/commit/f08cc9d). |
| 05-24 | Dashboard 2 POC — **decision deferred** | Spiked Node-RED Dashboard 2 (`@flowfuse/node-red-dashboard` v1.30.2) on the Pi to evaluate as a /ui replacement that "renders responsively to window size." **Installed alongside D1** (zero risk — separate package, separate URL, separate node types): D1 still at `/ui`, D2 at `/dashboard`. ~22 commits migrating just the **Solar Conditions** card alone (one of 13). What works: dense unified ui-template with mini SVG gauges, RSG badge boxes, MUF/foF2/X-ray tiles, 11-row HF band heatmap (formula identical to D1's Solar Panel script), custom click-to-collapse header showing inline summary (SFI/K/A) when closed, `:has()`-based CSS to shrink the widget slot on collapse, global CSS override to suppress group bezel/padding. **What hurt:** D2 ui-gauge can't render value text at width<2 (had to swap for custom SVG anyway, killing the "less custom code" justification); cross-tab wires silently drop; `flow.solarState` is tab-scoped so fanout had to move to Solar tab; default `gtype` is `gauge-half` not `gauge`; ui-text format is `{{msg.payload}}` not `{{value}}`; D2 has no native collapsible groups + no dynamic widget-height messages on ui-template. **The architectural problem (per chat at end of session):** both D1 and D2 use a fixed grid with hardcoded `width: N, height: N` per widget. Neither is *actually* responsive — the "responsiveness" we got was hand-tuned breakpoints. True responsive (CSS Grid `auto-fit minmax()` + `clamp()` + container queries) needs a real SPA via uibuilder or a standalone Vue/React app. Operator deferred the call ("later"). **Current repo state:** D2 install + Solar POC live on `/dashboard`, D1 `/ui` 100% unchanged. Easy unwind = `npm uninstall @flowfuse/node-red-dashboard` + remove the d2_* nodes (all prefixed `d2_` for grep). Tab `D2 Solar` exists but the actual widget + fanout + tick now sit on the original Solar tab `590e889d44815afb` (needed flow-context scope). Files to look at next time the decision comes up: this entry, README's "## Documentation map", commits `76238d0` (skeleton install) → `59bced5` (last D2 tweak). Don't rebuild on D1 incrementally — the more we patch D1 the more the eventual cutover costs. |
| 05-23 → 05-24 | Dashboard layout overhaul (multi-session) | ~50 commits across two sessions reshaping the Shack tab into a clean uniform-grid layout. **Group widths:** all 13 groups normalised to **width 8** (was a mix of 6/8/10/12/18). Heights tuned per group (Lightning shorter, RPi Fleet taller for 3 hosts, DXCC settled at 12 after 18→13→9→12 iterations). **Tab moves:** DXCC Tracker + DXCC Alerts moved onto VU2CPL Shack tab (DXCC Alerts last); AS3935 Tuning moved to Shack Monitoring tools tab. **Header simplified:** removed weather conditions + hourly forecast row from `Header — Clocks + Weather` (`eee1a8b8552aa21f`) — now only callsign + UTC + IST + sunrise + sunset. **Lightning alert box:** simplified to two buttons only — `ANTENNA ON` (green) + `BYPASS` — with BYPASS styled grey/inactive when off and **amber `#b58a00` / `#2d2300` / `#ffd45e`** when active (exact match to bypass banner). State sync via existing `paintBypass()` + `bypBtn` watcher. **Rotor card — full rebuild:** (a) Compass SVG widened to width 8 + new vintage aviation HUD cyan styling with rose + tick marks; (b) 9 separate ui_button preset nodes (EU/N/US/JA/VK/ZL/SA/W + STOP) **deleted** — replaced with SVG `<g>` button overlays at the exact compass angles using `id="btn-xx"` + click handler in `Click → Heading` function; added new **E (90°)** preset, moved ZL from 150°→170°, EU 340°→325°; (c) `HDG Display` ui_template removed (heading now shown in compass centre); (d) new combined **Rotor Controls** ui_template = LP/SP + numeric input + GO on one row, wired via `fetch+http-in` pattern with two endpoints (`/rotor/preset`, `/rotor/manual`); GO auto-powers the rotor before sending the heading; LP/SP falls back to current rotor heading if no target is set; (e) **Rotor power knob** — aviation-style toggle (TOGGLE not ON-only), positioned outside the compass top-right, click compass centre also powers ON with 5-min auto-off; MQTT topic set on node config (no Build Rotator String routing). **Scrollbars:** dark theme globally via `color-scheme: dark` + webkit/firefox custom styles in `ui_base` CSS. **UI widgets:** existing dashboard widgets expanded from width-6 to width-8 to fill the new uniform groups. Net feel: phone/tablet/desktop all render the same 2-column grid cleanly; everything that grows (event logs, DXCC alerts table, RPi fleet cards) scrolls internally instead of bloating its host group. |
| 06-04 | Fork tooling: MQTT broker config-node patch + hardware-tailored dashboard cards | **Bug:** Stage 13 only patched `const MQTT_BROKER` in the Init Defaults function node, never the two `mqtt-broker` *config* nodes (`f4785be9863eab08` + `mqttbroker.shack`) — so a forker's 37 mqtt nodes kept dialing `192.168.1.169` and showed disconnected ("broker not filled in"). A function node can't reconfigure a config node at runtime. Fixed: patcher now rewrites `broker` on every mqtt-broker config node. **Feature:** hardware-tailored dashboard. New `const CARDS = {…}` block in `uibuilder/shack/src/index.js` with `v-if` per card (build stamp v10→v11); Stage 13 asks a 12-question "which subsystems do you have?" round and flips the flags. Dependency-locked: R1 forces Power on if any of Lightning/Rotator/Flex stay (they switch outlets through it); R2 warns if Flex dropped while Lightning stays (TX-inhibit no-ops). For the 5 stand-alone subsystems (SPE/LP-700/Solar/DXCC/RBN) it also sets `disabled:true` on the flow tab to stop background polling. Scope: Vue for all 12, D1 only for those 5 (the other 7 are entangled or share a D1 group). Nothing deleted — fully reversible by flipping flags / re-running Stage 13; backs up flows.json+index.js first. Verified by extracting the real Python patchers and dry-running them (3 cards off → correct tabs/broker/CARDS; re-run all-on → idempotent re-enable; node count 501→501). |
| 06-08 | **Vue `/shack`: brand the remaining VU2CPL hardcodes for forks** | Audited `uibuilder/shack/src/`. Headline: **no network hardcodes** — all `fetch()` relative + uibuilder Socket.IO to `location.host`, so `/shack` always talks to its own Node-RED (nothing there breaks a fork). What remained was cosmetic branding. Stage 13 now also brands: `index.html` `<title>`, `manifest.json` description, `LightningCard` callsign/grid state defaults (it already did TopBar + manifest name). Bumped `index.js` cache-buster `?v=10`→`?v=11` (had drifted from build stamp v11). Two fork-specific spots that can't be auto-patched (NetworkCard host list + DXCC `clusterNames`) are now `// FORK:`-commented in index.js + documented in FORK_GUIDE A5.3. Verified bash -n + node --check + dry-ran patches (K1ABC → title/manifest/Lightning branded; clusters/IPs intact). No flows.json change. |
| 06-08 | **rotator-remote standalone web UI (Part B of the 06-08 handover)** | Gave the rotator gateway its own compass control page at `http://<pi>:8090/`, like spe-remote's `:8888/` — independent of Node-RED, just another `/ws` client. Done in the `vu2cpl/rotator-remote` repo (commit `7fc5411`): `app.py` serves `web/` (NoCacheStaticFileHandler, `/ws`+`/healthz` kept); `web/{index.html,app.js,style.css}` = heading readout + click-to-slew SVG compass + goto/stop/lpsp + presets + status pill. WS plumbing lifted from spe; JSON protocol unchanged; no build step. Verified locally (serves `/` + assets + healthz). Power out of scope (Tasmota/MQTT). Shack-repo change is only the CLAUDE.md infra-row cross-ref. |
| 06-08 | **Fork fix: sweep ALL `.169` broker hardcodes** | Follow-up to the 06-06 broker fix (which only did the mqtt-broker config nodes). The shipped `192.168.1.169` literal still survived in `monitor.sh`, `as3935_mqtt.py`, the LP-700 ws-client path, and the Init Defaults const — so a fork at a different IP had broken telemetry + LP-700 link. Swept all: `monitor.sh`/`as3935_mqtt.py` read `MQTT_BROKER` from env; `as3935.service` gets `EnvironmentFile=-/etc/default/vu2cpl-shack` (+ the long-missing `PYTHONUNBUFFERED=1`); Stage 7 broadened to rewrite ws-client paths → `localhost` (gateways are local, like SPE/rotator already are) + the Init Defaults const, with a **fail-loud residual sweep**; Stage 9 writes `/etc/default/vu2cpl-shack` (`MQTT_BROKER=<inventory IP>`) and sed-retargets both units' `User=vu2cpl`/`/home/vu2cpl` → `$ACTUAL_USER`. flows.json left at `.169` (Stage 7 rewrites at install). Verified bash -n + py_compile + dry-ran sweep (fork → 0 residual; .169 → skip) + sed retarget (vu2cpl-shack path intact). Full write-up: `FORK_BROKER_HARDCODE_FIX.md`. |
| 06-06 | **rebuild_pi.sh: MQTT broker IP → mandatory inventory + always-patched (Stage 7)** | Operator: "MQTT not working on a fresh fork despite giving the correct IP." Root cause: the broker IP was collected/applied **only inside Stage 13's opt-in customize** — a bare Enter past it left both `mqtt-broker` config nodes at the shipped `192.168.1.169`, so all 37 mqtt nodes dialed a broker that doesn't exist on the fork. Fix: broker IP now asked in the **mandatory up-front inventory** (`ask_broker_ip`, default = Pi's own IP, persisted as `MQTT_IP`), and **Stage 7 (always runs) patches both config nodes + restarts Node-RED** — so MQTT works even if customize is skipped. Stage 13 no longer re-prompts (reads `MQTT_IP`). No-op on VU2CPL's own rebuild (Pi IP == .169). Verified bash -n + dry-ran the patch (both nodes → target IP) + inventory round-trip. Recovery for the operator's broken Pi: `--stage 7` or pencil-edit the broker in the editor. Docs: CLAUDE broker note, REBUILD_PI/FORK_GUIDE inventory+Stage7 rows. |
| 06-06 | **rebuild_pi.sh: spe-remote stage + ask-hardware-once** | Operator caught that the rebuild installed `lp700-server` + `rotator-remote` but **never `spe-remote`** — a fresh rebuild left the SPE gateway uninstalled. Added Stage 13c (`spe-remote`, `:8888`, clones `vu2cpl/spe-remote`). Plus the "one fix for good": eliminated redundant prompting — new `hw_inventory()` asks the 12 "which subsystems?" questions **once** up front (after pre-flight), persists to `$HOME/.rebuild_pi.hw`, applies dependency rules; all three gateway stages (11/13b/13c) + Stage 13's CARDS patching read it, none re-prompt. Stage 13 lost its own hardware round; verify is now `--stage 16` (7 critical + 6 optional, +spe check). `--reset` wipes the inventory too. Verified bash -n + dry-ran persist/load/gate. Docs: CLAUDE infra row, REBUILD_PI pauses rewrite, FORK_GUIDE inventory + 13c rows. |
| 06-06 | **Rotator → WebSocket gateway (TODO #31 closed)** | Lifted Rotor-EZ serial control out of Node-RED into a new standalone repo [`vu2cpl/rotator-remote`](https://github.com/vu2cpl/rotator-remote) (Python/Tornado, `:8090`), mirroring spe-remote / lp700-server. **Phase 1:** gateway built (daemon reader thread + asyncio poll/command writers, single port owner, multi-client broadcast); protocol bytes extracted verbatim from the old flow; offline unit-tested (venv: command bytes, reply parse, split-frame buffering, command translation) + **bench-verified on the real rotor** — `goto 90` turned it clockwise through north, `stop`/`lpsp`/multi-client all good. **Phase 2:** Node-RED Rotator tab → ws-client ([`626ccc0`](https://github.com/vu2cpl/vu2cpl-shack/commit/626ccc0), 501→497): −8 serial-path nodes, +ws-client trio + `Parse Rotator WS`; `Send Rotator → WS` translates the `AP1NNN\r`/`;` strings (Build Rotator String + Click→Heading unchanged) into command JSON. Power untouched (Tasmota/MQTT). **Phase 3:** `rebuild_pi.sh` Stage 13b (opt-in install, serial-port prompt, healthz) + doc CDP (CLAUDE/REBUILD_PI/FORK_GUIDE/README/this). Removes the "restart Node-RED to free the serial port" friction; unblocks the Mac app (#6). Planned + approved in plan mode; verified phase-by-phase with operator on the Pi. |
| 06-04 | rebuild_pi.sh Stage 1: bullseye fallback for raspi-config do_serial + preflight shows OS | Third Stage-1 fix today, this time on a forker's Pi (`adersh@raspberrypi`) running Pi OS Bullseye. `raspi-config nonint do_serial_hw 0` is bookworm+ only; bullseye uses `do_serial` with combined arg semantics (`do_serial 2` = no console + hw on). Script died with `do_serial_hw: not found` and `set -e` killed the run. Fix: try modern call first, fall back to bullseye form, warn-not-fail if both miss (operator can finish via interactive `sudo raspi-config`). `do_i2c 0` got a matching `|| warn` guard for symmetry. **Bonus** — preflight now prints `Pi OS: <pretty-name> (<codename>)` via `/etc/os-release` so the next debugging pass sees the OS version at a glance; bullseye/buster also gets a soft warning that they're past Pi OS current-stable. Lesson: cheap observability that should have been in the script from day one. The noderedpi5 saga kept reinforcing the same point — every minute the operator spends inferring "WHY did this fail?" is a minute the script should have spent telling them up front. |
| 06-04 | rebuild_pi.sh Stage 1: detect pending-reboot after dist-upgrade | Follow-up to the I²C-softening earlier today. Operator hit `modprobe: FATAL: Module i2c-dev not found` on the next attempt. Root cause: Stage 1's `apt dist-upgrade` (very first step) almost always picks up a new `raspberrypi-kernel*` package on a fresh Pi-OS image — new kernel's modules land in `/lib/modules/<new>/` but old kernel is still running, so `/lib/modules/$(uname -r)/` is stale and `modprobe` returns FATAL for every module including `i2c-dev`. Same mechanism breaks any post-dist-upgrade modprobe + the dtparam from raspi-config. Pi OS drops `/var/run/reboot-required` to signal this exact state. Fix: Stage 1 now checks for the marker file IMMEDIATELY after dist-upgrade, before any later step can hit the confusing FATAL. If present: prints red ✗ with explanation of what's broken (modprobe / dtparam / udev), prints the recovery sequence (`sudo reboot` → `bash rebuild_pi.sh`), exits 0 WITHOUT marking Stage 1 complete (so resume replays it — dist-upgrade is idempotent). REBUILD_PI.md Step 2 also got a `ls /var/run/reboot-required` check between dist-upgrade and the apt-install step, with the same recovery callout, so the manual path tells the same story. Same philosophy as the noderedpi5-saga softening: fail loudly with actionable diagnostics, not silently with cryptic errors. |
| 06-04 | rebuild_pi.sh Stage 1: I²C verify softened | Operator hit `[[ -e /dev/i2c-1 ]] \|\| fail "/dev/i2c-1 not present after raspi-config"` on a fresh Pi. Two-cause brittleness: (1) `raspi-config nonint do_i2c 0` sets the dtparam but doesn't auto-load `i2c-dev` kernel module on some Pi-OS images; (2) udev populates the device node async, so even when the module IS loaded the check can fire before the node appears. Fixed: stage now `modprobe i2c-dev` + 5×1s retry loop, then downgrades miss to warn-not-fail with diagnostics (lsmod / dtparam grep / `/dev/i2c-*` listing) and a plain-English note that only the **legacy** `as3935.service` Pi-side daemon needs `/dev/i2c-1` — that daemon has been standby-fallback since the ESP32 bridge went live 2026-05-11, so normal shack operation is unaffected and a fleet-monitor-only Pi is no longer blocked. Same philosophy as the noderedpi5-saga softening: every `fail` that isn't strictly tied to the end-user goal becomes `warn` with diagnostics. REBUILD_PI.md Step 2 also gained a `sudo modprobe i2c-dev` line + callout matching the script's warn message. |
| 06-04 | flows.json audit — 8 dead nodes pruned + 2 group renames | Audit pass: 509 → 501 nodes. Dropped 2 never-instantiated subflows + their internal functions (`66ca1554.e4c85c`, `a7edb3ab6224c12f`, `f0932848.b39158`, `82b5c109e09d7a14`); 2 runtime-disabled DXCC functions whose live siblings already carry the wires (`d5eca40d3503035d` Format Telegram Alert → `94b77826079bad57` Dedup 10 minute; `9fd52c02a8486dce` Fetch All Modes + Parse → `aa7434df62b95ebc` lotw only); 1 orphan 7-seg-LED formatter (`8778c5c78701ce17` Flex Frequency Display); 1 orphan flow-var setter (`0c1611c32c969490` Skimmer Config — consumers had inline `\|\| 'VU2CPL'` fallbacks). 3 wire prunes (Classify → dead formatter ×2; Build Club Log → dead Fetch ×1). 2 unnamed groups (`?`) renamed: Solar tab's 33-node group → "Solar fetch + parse + dashboard"; Internet tab's 15-node group → "Network ping + dashboard". CLAUDE.md DXCC key-IDs row updated to point at the live Fetch node. Zero behaviour change — all deletions were either disabled or had no producer. Audit method preserved in SHACK_CHANGELOG entry for future re-runs. |
| 05-26 (evening) | **Lightning TX inhibit + Telegram retrigger differentiation** | Two interlocking enhancements to the lightning-disconnect chain ([`d87a708`](https://github.com/vu2cpl/vu2cpl-shack/commit/d87a708)). **(1) FlexRadio TX inhibit on disconnect**: when the matrix fires DC, also send the radio `['xmit 0', 'transmit set inhibit=1']` via its TCP API so the operator can't accidentally key into a disconnected antenna. Cleared on reconnect/force-reconnect/manual override/HTTP ANT ON/bypass ON with `transmit set inhibit=0`. Radio stays powered + RX-capable; only TX is blocked. **Command lineage was non-obvious** — initial `interlock tx1_inhibit=N` returned `status_code=0x5000002D` (command not recognised); two debug iterations sprayed multiple known forms with a status-code logger ([`e16646e`](https://github.com/vu2cpl/vu2cpl-shack/commit/e16646e), [`34c1db4`](https://github.com/vu2cpl/vu2cpl-shack/commit/34c1db4)) — `transmit set inhibit=N` was the only `status_code=0` form for FLEX-6600 SmartSDR v3.x. Final lock-in [`0722d41`](https://github.com/vu2cpl/vu2cpl-shack/commit/0722d41). Full lineage table + spray-and-pray pattern documented in CLAUDE.md so a future firmware change that breaks this can be re-derived quickly. Why TX-inhibit and not full power-off: the existing `RADIO_ENABLED=true` mode in Init Defaults cuts radio mains via Tasmota which kills RX too — operator loses storm-band situational awareness during the very period they most want to be listening. TX-inhibit gives the same safety guarantee without the RX blackout. Implementation: 2 new nodes on Lightning tab (`tx_inhibit_setter_01` function + `flexradio_tx_inhibit_01` flexradio-request; reuses existing `flexradio-radio` config node 94d0df28ae5cccfc cross-tab, no new TCP connection); 5 new wires from Trigger Disconnect/Execute Reconnect/Force Reconnect/HTTP→Antenna ON/Manual Override into the Setter; the Setter inspects msg.cmd ('DISCONNECT' → 1; 'RECONNECT'/'BYPASS_ON' → 0; else null) and emits the interlock command for the flexradio-request to send. Idempotent — DC retriggers within the 20-min reconnect window safely re-fire `=1`. **(2) Telegram retrigger alert format**: pre-fix every DC during a storm sent the same `⚡ ANTENNA DISCONNECT` alert (5+ duplicates in a single 30-min storm with strikes every 5 min during reconnect window — noisy + operationally useless). `Trigger Disconnect` now sets `msg.retrigger = (antenna_off was already true)`; `Format Log` propagates it to event_record; `tg_lightning_router` branches: first DC = existing "ANTENNA DISCONNECT" format + new "TX inhibited" line; subsequent DC = NEW `⚡ STORM CONTINUES` alert reading "Antenna already OFF; TX still inhibited. Reconnect timer reset to NN min from now" (NN reads `flow.reconnect_min` live). Reconnect alert also gains a "TX allowed again" line for symmetry. Net: one initial DISCONNECT per storm, STORM CONTINUES for each additional strike (each useful — confirms storm hasn't ended), one RECONNECT when all-clear timer fires. Rate-limiting (5-per-60s per type) unchanged. CLAUDE.md Lightning Antenna Protector section gained two new subsections documenting both. flows.json 502→504 nodes. Verification path: fire `TEST ⚡ 6 km DISCONNECT` inject → antenna OFF via Tasmota AND radio refuses TX (SmartSDR shows red TX Inhibit indicator) AND Telegram fires DC alert with "TX inhibited"; within 20 min fire second test → no new DC alert, get STORM CONTINUES with timer-reset wording instead; wait for reconnect timer or click Force Reconnect → antenna ON, TX inhibit clears, SmartSDR indicator goes dark, RECONNECT alert. |
| 05-26 (afternoon) | **DXCC VU2CPL cluster offline fix + LIVE/OFFLINE pill saga + iPhone topbar + FORK_GUIDE Stage O** | Four interlocking pieces of work captured here so a future session sees the arc end-to-end. (1) **VU2CPL DXCC cluster pill showed offline** in the /shack DXCC card even though the cluster server was up and serving spots ([`e188e9d`](https://github.com/vu2cpl/vu2cpl-shack/commit/e188e9d)). Cause: `login-parse-dedup-v2` treated each TCP chunk as one DX line, but CwSkimmer batches `cluster ack + 2-3 DX spots` into one TCP segment. The first chunk after login started with `VU2CPL-1 de SKIMMER ...` (not `DX de`), so the existing `startsWith('DX de')` check returned `null` and silently dropped every embedded spot. cluster3 never got a `lastSpotTs` bump → watchdog kept `connected: false`. N2WQ / VU2OY / VE7CC happened to deliver one spot per chunk so they worked. RBN tab's parallel handler already had the multi-line fix (added 2026-05-17); the DXCC tab never got it. Refactored to split chunks on newlines and `node.send` one msg per `DX de` line, plus one cluster_status update per chunk. Mute-check also got a `msg.topic` fallback because tcp-in client-mode doesn't reliably populate `_session.host`. (2) **LIVE/OFFLINE pill saga — 5 iterations** ([`0834b41`](https://github.com/vu2cpl/vu2cpl-shack/commit/0834b41), [`3822489`](https://github.com/vu2cpl/vu2cpl-shack/commit/3822489), [`479ef10`](https://github.com/vu2cpl/vu2cpl-shack/commit/479ef10), [`77e9059`](https://github.com/vu2cpl/vu2cpl-shack/commit/77e9059), [`85ab62f`](https://github.com/vu2cpl/vu2cpl-shack/commit/85ab62f)). Pill stuck on red even though cards were receiving data. Tried `uibuilder.ioConnected` direct read (i1), `onTopic` monkey-patch heartbeat (i2), no-cache HTML meta tags for PWA caching (i3), then the breakthrough — a **visible diagnostic strip** showing per-signal counters next to the OFFLINE pill (i4). Strip showed every signal firing (`oT28 oM29 dS28 ioC=Y · 0s`) while pill stayed OFFLINE → bug was in the state-to-pill propagation, not heartbeat collection. Vue's reactive ref-to-prop chain from App.setup to TopBar prop was silently breaking. Final fix (i5): moved the `connected`-state derivation INTO TopBar's already-working `tick()` setInterval, dropped the parent-to-child prop binding entirely. **Lesson:** when you can't reason about which of N competing signals is breaking, make every signal visible on the device. The diag-strip diagnosed the bug in one screenshot's worth of feedback. Architecture left in place: `window.__shackLastMsgAt` (heartbeat), `window.__shackPaths` (per-signal counters), `window.__shackBuild` (visible build stamp distinguishing "code didn't load" from "code loaded but signal broken"). (3) **iPhone topbar overflow** ([`22d75c8`](https://github.com/vu2cpl/vu2cpl-shack/commit/22d75c8)). iPhone portrait (~390px wide) couldn't fit callsign + LIVE pill + 4 clocks (UTC/IST/Sunrise/Sunset) in one row; Sunrise + Sunset overflowed off-screen. Fixed via `flex-wrap: wrap` on `.topbar` + `.clocks`, `flex: 1 1 auto; min-width: 0` on `.topbar__left`, and `@media (max-width: 480px)` mobile-specific tightening (smaller callsign font, 4 clocks splitting full width with `justify-content: space-between`). Mac/iPad-landscape layouts unchanged. (4) **FORK_GUIDE Stage O** ([`3b909df`](https://github.com/vu2cpl/vu2cpl-shack/commit/3b909df)). Per-device PWA install runbook for a forker's users (family / club / co-ops) — Operator pre-flight + Hand-out template + per-platform install steps (Mac/iPad/iPhone/Android/Windows-Linux) + "what users see after install" + per-device troubleshooting table indexing all 5 bugs we hit this afternoon (stale Safari favicon cache on iOS, Mac dock-icon HTML cache, iOS `max-age=0` quirk, iPhone topbar overflow, pill permanently OFFLINE). Cross-linked from Stage I. |
| 05-26 (morning) | **D2 retired + FORK_GUIDE added** | Two-pronged cleanup: (1) **Dashboard 2 (`@flowfuse/node-red-dashboard`) gone.** The 2026-05-24 POC was abandoned in favour of uibuilder + Vue — D2's grid is fixed not responsive, `ui_control` missing from `node-red-dashboard@3.6.6`, cross-tab wires silently drop, `ui-gauge` can't render value text at width<2 so custom SVG was needed anyway. 9 `d2_*` nodes stripped from flows.json (1 tab + 1 ui-base + 1 ui-page + 1 ui-theme + 1 ui-group + 1 function + 1 inject + 2 ui-templates) via a Python pass that also removed 1 stray wire reference; 511→502 nodes; regex-verified zero `d2_*` left. `npm uninstall @flowfuse/node-red-dashboard` on the Pi cleared 25 transitive deps + ~80 MB node_modules. `/dashboard` URL went 200 → 404 cleanly; `/ui` (D1) and `/shack` (Vue) unaffected (curl-verified post-restart). Misleading code comment in `as3935_tuning_replay_fn` rewritten (was: "or we migrate widgets to Dashboard 2"). REBUILD_PI palette install + `rebuild_pi.sh` packages array gained `node-red-contrib-uibuilder` with a callout warning against reinstalling D2. CLAUDE.md palette list updated, added "Retired packages" subsection covering both this retirement and the older `@gdziuba/node-red-usbhid` removal. (2) **New `FORK_GUIDE.md`** — 350-line top-level runbook for a different operator forking the repo to run at their own QTH. Pattern matches existing docs (REBUILD_PI=same-Pi rebuild, DEPLOY_PI=fleet member, FORK_GUIDE=different station). Stages A–N cover per-site customisation: network/MQTT broker, Lightning Init Defaults (callsign/grid/antenna power switch/radio enable/thresholds), DXCC + Telegram credentials + cluster selection, FlexRadio, SPE, rotor, LP-700, Tasmota devices (topic renames + TZ + power-card layout), `/shack` PWA rebadge with full icon-regeneration command list (`rsvg-convert` + PIL multi-res `favicon.ico`), RPi fleet, network monitor, GPS NTP, Open-Meteo location, backups + secrets hygiene. Hardware-compatibility table up-front + troubleshooting table at end. REBUILD_PI prologue + README "Documentation map" + repo-layout tree all cross-link to it. REBUILD_PI verification checklist grows 14 → 15: new check #4 is "`/shack` Vue dashboard loads" with diagnostic guidance for uibuilder-missing / src-files-missing cases. Commit [`13c9ac6`](https://github.com/vu2cpl/vu2cpl-shack/commit/13c9ac6). |
| 05-25 → 05-26 | **uibuilder Vue dashboard — full 12-card migration** | After the D2 POC stalled (see prior row), the operator committed to a real responsive SPA. Installed `node-red-contrib-uibuilder` v7.6.2 on the Pi; root at `~/.node-red/projects/vu2cpl-shack/uibuilder/` (lives in this repo). One uibuilder node `uib_shack_01` serves `/shack`. Front-end: **self-hosted Vue 3** (`vue.global.prod.js` copied into `uibuilder/shack/src/`, no CDN/Vite build step). 63 commits across two sessions producing **12 cards** in `src/index.js` (one big single-file Vue app with components: FlexCard · LP700Card · SPECard · RotorCard · LightningCard · PowerCard · SolarCard · DXCCCard · RBNCard · RPiCard · NetworkCard · GpsNtpCard). **Wiring pattern (uniform across all cards):** (a) each card has a `vue_<name>_tick_NN` inject (every 1-5 s) firing a `vue_<name>_builder_NN` function on the card's own Node-RED tab — reads `flow.<state>` (mostly file-backed) and emits `{topic:'<name>', payload:{…}}` to `uib_shack_01`; (b) Vue `uibuilder.onTopic('<name>', ...)` `Object.assign`s into a reactive state; (c) commands go out via `fetch()` to existing `/lightning/*`, `/dxcc/*`, `/rotator/*`, `/rotor/*` HTTP endpoints **OR** through `uibuilder.send({topic:'<name>/cmd', …})` → a per-card `vue_<name>_cmd_router_NN` that translates to the shape existing flow nodes expect (Power TOGGLE Router, ws_btn_router, Route CMD, AS3935 cmd MQTT-out, …). **Cross-tab state mirror:** AS3935 status/hb/last_event cache nodes write `global.set('as3935_<key>', …)` in addition to flow-scoped — so the Lightning-tab builder can read them. Same trick for Rotor (`flow.rotator_heading` added to `Slice heading`). **Design system:** sans-serif body (`-apple-system, ...Inter`), monospace only for numerics/callsigns/freqs/IPs/times/timestamps (`.mono` utility + `font-family: var(--font-mono)`), 8/12/16px spacing scale, calm GitHub-dark palette, `clamp()` typography (`--fs-xs: clamp(12px, 0.9vw, 14px)` → `--fs-xl: clamp(24px, 2.4vw, 32px)`), all in `src/index.css`. **Layout:** CSS column masonry (`column-width: 320px; column-gap: 12px`) so short cards don't leave empty vertical gaps under tall cards. Card order: Flex → LP-700 → SPE → Rotor → Lightning → Power → Solar → DXCC → RBN → RPi → Network → GPS. All cards default to **collapsed**; header shows colour-coded live summary (e.g. Lightning `ANT ON · 0km`, Flex `RX · 24.915 DIGU`, SPE `● ON · RX · 20M [▬░░] 80W` with mini power bar). **Top bar:** callsign + `● LIVE`/`OFFLINE` pill (inline, no longer fixed-position masking sunset) + 4 clocks (UTC / IST / Sunrise / Sunset). **Card-specific notes:** Lightning+AS3935 merged with click-to-reveal tunable help (`title` tooltips don't work on touch, so click-toggle pattern); DXCC has cluster mute (single-click toggles `global.dxcc_cluster_mute[name]` which Login+Parse+Dedup honours; shift-click for actual TCP disconnect deferred); Rotor compass is click-anywhere (xyToDeg via `getScreenCTM()`) with hover preview line + thicker green needle with arrowhead + 2×5 octant-coloured preset chip grid (hover lift + click flash + active glow); LP/SP button toggles label LP↔SP each click. **Coexistence:** `/ui` (D1, untouched), `/dashboard` (D2 Solar POC, ignored), `/shack` (Vue, primary going forward). All three URLs serve at once. Rollback: `npm uninstall node-red-contrib-uibuilder` + `rm -rf uibuilder/` + remove all `vue_*` nodes from flows.json. **Known gotchas captured during the work:** uibuilder uses `column-fill:auto` masonry which orders columns top-to-bottom; D2's `:has()` selector pattern doesn't apply; relative-path fetch from `/shack/…` to `/rotor/*` works because the leading `/` makes it root-relative; the 3 rotor endpoints are `/rotor/*` (no 'a') except power-toggle which is `/rotator/power-toggle` (existing inconsistency, not fixed yet — Vue handles both correctly). **05-26 wrap-up adds:** (a) **SPE auto-ranging power bar** ([`debb454`](https://github.com/vu2cpl/vu2cpl-shack/commit/debb454)) — operator clarified that "auto-scale per README" meant *detected-power-driven* auto-ranging, not the L/M/H ↔ pwrMax mapping the old `ws_format_state` was doing. Vue SPECard now picks the smallest rung from `[5,10,25,50,100,250,500,1000,1500,2000,5000]` that contains the current `pwr` reading; scale label renders as `/ 5 W` … `/ 1.5k W` … `/ 5k W`. Adds finer low-end rungs (5/10/25/50) over the legacy `/ui` SPE Panel ladder so tune carriers (~3 W) are actually readable. Don't recouple L/M/H to pwrMax — CLAUDE.md SPE section + README "SPE Amplifier" both rewritten to make this explicit. (b) **PWA / apple-touch-icon set** ([`debb454`](https://github.com/vu2cpl/vu2cpl-shack/commit/debb454), [`5897fab`](https://github.com/vu2cpl/vu2cpl-shack/commit/5897fab)) — for "Add to Home Screen" on iPad/iPhone and a non-Node-RED-red-dot favicon in desktop browsers. Source SVGs (`icon.svg` 180×180 + `icon-maskable.svg` 192×192 with safe-area padding for Android adaptive icons) hand-drawn: dark `#0d1117` rounded square with green signal arcs over an antenna mast and "VU2CPL / SHACK" lettering. Rasterised via `rsvg-convert` to `favicon-{16,32,48}.png` + `apple-touch-icon-180.png` + `icon-{192,512}.png`, then `PIL` built a multi-res `favicon.ico` (16+32+48). `index.html` link tags ordered most-compatible-first (ICO → PNG by size → SVG → mask-icon for Safari pinned tabs); cache-buster `?v=4`. `manifest.json` lists raster + SVG, with `purpose:"any maskable"` on 192/512. **Safari favicon-cache lesson** — Safari's favicon cache lives in `~/Library/Safari/Touch Icons Cache/` and is **not** cleared by Cmd+R, Empty Caches, or even `rm` (macOS Sequoia TCC blocks `~/Library/Safari/*` even with sudo unless Full Disk Access is granted). The reliable nuke is **Cmd+Q the Safari process** and relaunch — favicon cache lives in-process and rebuilds on next page load. Secondary path: Safari → Settings → Privacy → Manage Website Data → search host → Remove. Document this if a future favicon refresh "doesn't take" in Safari. (c) **CDP rule reminder** — the SPE auto-range work clarifies the value of CDP's Document step: this row alone updates HANDOVER + CLAUDE + README + SHACK_CHANGELOG in the same cycle as the code change. **05-26 evening adds three more fixes:** (d) **Lightning CAPE always-null** ([`662bb7b`](https://github.com/vu2cpl/vu2cpl-shack/commit/662bb7b)) — writer/reader key mismatch in `vue_lightning_builder_01`: read was `flow.cape_now`, writer (`Parse Open-Meteo → Strike`) was `flow.om_cape_latest`. Same class of bug as the 2026-05-10 DXCC `spot.dxCall` vs `spot.call` mismatch; pattern is "downstream renders nothing while upstream alive → first check key names match end-to-end." Reader updated + inline comment added warning future-self not to rename either side without touching the other. (e) **Lightning card duplicate display rows trimmed** ([`3497bea`](https://github.com/vu2cpl/vu2cpl-shack/commit/3497bea) + [`916a789`](https://github.com/vu2cpl/vu2cpl-shack/commit/916a789)) — dropped Callsign / Grid / Threshold / Reconnect / Antenna rows from the expanded stats `<dl>` because each was already visible elsewhere (top bar / Thresholds collapsible / collapsed-header chip). Stats grid now three operational rows only: Total strikes, zone breakdown, Closest. State fields kept in reactive state because sliders + collapsed-header chip + builder defaults still read them. Open follow-up — antenna state isn't visible in the *expanded* header (collapsed-summary chip is `v-if="!expanded"`); deferred. (f) **DXCC seed age** ([`ea0e361`](https://github.com/vu2cpl/vu2cpl-shack/commit/ea0e361)) — `vue_dxcc_builder_01` had `seedAge: null` hardcoded; now `fs.statSync` reads `nr_dxcc_seed.json` mtime (= last Club Log fetch since the daily 02:00 cron rewrites it) and formats as relative time (`just now` / `Nm ago` / `Nh ago` / `Nd ago` / `Nmo ago`). Adds `libs: [fs, os]` declaration. Also emits `stats.seedMtime` (epoch ms) for future use. Verified live — Seed field flipped from `—` to `3h ago` post-restart at ~05:00 IST. |
| 06-19 | **SPE Tune-LED indicator on both dashboards (#34)** | Surfaced the amp's front-panel TUNE LED on D1 `/ui` + Vue `/shack`. Gateway side was already done — spe-remote decodes byte 4 bit 6 of the RCU frame into `tune_active` and broadcasts it (RCU auto-enabled), so this was Node-RED + Vue only. `ws_format_state` emits `tune`; the **Tune button fill toggles** neutral→amber to mirror the LED (D1 + Vue), Vue also shows a `⚡ TUNE` collapsed-header chip. First cut (v12) only toggled a box-shadow on an always-amber button → looked like "no indication"; revised same day to a real colour change (build **v13** / `?v=13`). **Confirmed working on-amp** (operator ran a real TUNE — button goes amber while the LED is lit, clears after) → **#34 closed**. Also a `v14` copy fix: the Vue Tune confirm dialog now reads "Transmit a low-power tuning carrier within a few seconds to start tuning" (the operator keys the carrier, not the amp). |
| 06-19 | **Fork fix: mosquitto `status=3` from duplicate `persistence_location`** | Forker `adersh@NodeRed` ran `rebuild_pi.sh`; `mosquitto.service` failed to start (`status=3`, "Start request repeated too quickly"). Foreground run named it exactly: `Error: Duplicate persistence_location value` between `conf.d/lan.conf:5` and `mosquitto.conf:13`. **Root cause — the "two locations" trap:** the stock Bookworm `/etc/mosquitto/mosquitto.conf` already sets `persistence` / `persistence_location` / `log_dest` and `include_dir`s our drop-in on top; Stage 2's `lan.conf` re-declared the same three, and mosquitto 2.x treats a duplicate `persistence_location` as **fatal**. (VU2CPL's own broker predates the script + was hand-configured, so the dup only surfaces on a clean fork install.) **Fix** (`stage_02_mosquitto`): `lan.conf` now carries **only** `listener 1883` + `allow_anonymous true`; a `grep`-guarded defensive branch appends the persistence lines *only if* the stock conf lacks them (so a minimal install still persists retained msgs, never duplicates). Verify step now dumps `mosquitto -c … -v` + journal tail on failure so the next forker sees the real error, not "not active". Docs: REBUILD_PI Step 3 trimmed + callout + new failure-mode row; SHACK_CHANGELOG 2026-06-19. No flows.json change. |

---

## Current system state

| Subsystem | State |
|-----------|-------|
| AS3935 chip + I²C + IRQ wire to GPIO4 | ✅ Verified working (209 noise events caught during 05-01 storm at NF=0; TRCO+SRCO both calibrated OK) |
| AS3935 publisher | ✅ ESP32 bridge ([`vu2cpl-as3935-bridge`](https://github.com/vu2cpl/vu2cpl-as3935-bridge)) live since 2026-05-11; **v0.2.0** since 2026-05-12 adds an MQTT cmd channel (`lightning/as3935/cmd` + `cmd/ack`) covering all tunables + `calibrate_tun_cap` action, NVS-persisted, range-validated. Indoor `as3935.service` on noderedpi4 stopped + disabled (kept as fallback). MQTT status/hb/event contract still wire-compatible with the original Python daemon |
| Disconnect logic | ✅ **Distance-graded as of 2026-05-12, regression-fixed 2026-05-13.** 3×3 matrix (AS3935 close/medium/far × OM cold/lit/severe). OM no longer fires DC directly — only corroborates AS3935 hits in medium / far zones. Close zone (<10 km) still single-hit-fires. 7 cfg keys in Init Defaults. Full matrix table in CLAUDE.md "Lightning Antenna Protector" section. **2026-05-13 audit** found yesterday's `76d60e5` had silently broken real AS3935 disconnects via a `source !== 'AS3935'` strict-equality filter (actual source string is `'AS3935 (local)'`); event log + JSONL kept writing "DISCONNECT triggered" while no MQTT was actually published. Filter relaxed to reject only `Open-Meteo`; AS3935 / TEST / future sources all pass through the matrix. Same audit bundled 10 follow-up fixes (dead `Within threshold?` switch removed, reconnect-timer cancellation on manual paths, bypass-aware disconnect log, 3 stray `node.warn`s stripped). Lightning tab 76 → 75 nodes. SHACK_CHANGELOG.md 2026-05-13 entry has the full account. |
| AS3935 Control Panel | ✅ New ui_template on flow tab `AS3935 Tuning` (id `fe70cfdcdfa19aa4`); dashboard group `as3935_ctl_grp`. Exposes NF / WDTH / SREJ / TUN_CAP / Mask dist / AFE GB / Min strikes / Modem sleep + actions (Calibrate TUN_CAP / Republish / Reboot / Factory Reset WiFi). Matches GitHub-dark palette + Pattern-B IIFE (`(function(scope){…})(scope)`) |
| Open-Meteo CAPE polling (every 5 min) | ✅ Auto-fires disconnect when CAPE ≥ 800 J/kg or `weather_code ∈ {95,96,99}` |
| Antenna + radio auto-disconnect chain | ✅ Verified end-to-end during 05-01 storm |
| Dashboard refresh persistence (log + map + AS3935) | ✅ All three replay within 30 s of page load |
| Master Dashboard handlers | `as3935_ready`, `as3935_hb`, `as3935_status`, `bypass_state`, `log`, `strike` (incl. `as3935`) all present |
| Lightning Protection card | Lives at bottom of Shack tab (group `vu2cpl_grp_lightning`, width 12, order 9). Lightning detect tab deleted. |
| Bypass switch | Off on every Node-RED launch (Init Defaults). Auto-expires after 120 min. State persists across page refresh via 30 s replay. Verified TD bypass early-out for both Open-Meteo and AS3935 strike paths. |
| LP-700 telemetry | ✅ Via `lp700-server.service` (port 8089) → Node-RED ws-client. ~25 Hz updates. Multi-client capable. `@gdziuba/node-red-usbhid` still installed but unused; uninstall after 1 week of stable WS operation. |
| Rotator control | ✅ Via `rotator-remote.service` (port 8090) → Node-RED ws-client since 2026-06-06. Owns the Rotor-EZ FTDI serial port; Node-RED no longer touches it. Bench-verified. Power separate (Tasmota/MQTT). Repo `vu2cpl/rotator-remote`. |
| HA Pi monitoring | ✅ Implemented via HA-side automation (no Node-RED changes — `mqtt.publish` to `rpi/HassPi/*` every 30 s) |
| Pi GPS NTP server (`gpsntp.local`) | ✅ Stratum 1, PPS error ±152 ns, system clock 35 ns slow of GPS truth. Independent repo (`pi-gps-ntp-server`). Tapped off the U3S's QLG1 via the 6-way header with 5 V → 3 V dividers; U3S unaffected. Mac configured to use it as primary NTP source via `systemsetup -setnetworktimeserver gpsntp.local`. |
| gpsntp on RPi Fleet Monitor | ⚠ Pi side ready (`rpi-agent.service` active, MQTT telemetry publishing, HTTP probe 404 OK). Node-RED `httpDevices` map needs an entry for `gpsntp` to surface Reboot/Shutdown buttons — see follow-up #9. |

---

## Open follow-ups

| # | Item | Notes |
|---|------|-------|
| 35 | **UberSDR Telegram alerting** | **Deferred 2026-07-01** (operator: "will do it later"). The UberSDR dashboard (flow tab `ubersdr_tab`) landed 2026-07-01; a Telegram alert on it was scoped but parked. Detection must live in **Node-RED** — only it has the aggregated `ubersdr_agg` payload, which already carries an `online` flag + `cpuPct` + `listeners` + per-band noise, so a small monitor function on that output + a Telegram/MQTT-out node on the UberSDR tab is all it takes. **Two decisions still open:** (a) **trigger** — recommended first alert was receiver **offline/back** (sessions topic stale >60 s → 🔴 offline / ✅ back); other candidates: SDR CPU >85 %, listener-count milestone, per-band noise spike. (b) **recipients/path** — Manoj-only via the existing Node-RED shack Telegram pipeline (simplest, operator alert), OR the Pranav/Raji/Manoj family group via Node-RED → MQTT `shack/alerts/ubersdr` → HA `script.notify_all_telegram`, OR both. |
| 1 | Move AS3935 antenna **outdoors** | Hardware. **Major progress 2026-05-11**: ESP32 bridge ([`vu2cpl-as3935-bridge`](https://github.com/vu2cpl/vu2cpl-as3935-bridge) v0.1.1) running on the bench, indoor Pi daemon retired. Still indoors though — physical install (enclosure seal, 18650+TP4056+solar power chain, shade mount, post-install TUN_CAP re-tune) is the remaining work to regain the rated 40 km range |
| 2 | ~~AS3935 systemd unit hardening~~ | **Done 2026-05-10** (`c3f55a8`) — directives baked into the base `as3935.service` unit (simpler than a drop-in for a unit we own). `After=network-online.target mosquitto.service`, `Wants=network-online.target`, `Restart=on-failure`, `RestartSec=5`. Verified live |
| 3 | ~~Open-Meteo dashboard placeholder~~ | **Obsolete 2026-05-10** — the badge this referred to (`OPEN-METEO MONITOR · Waiting for data…` in the Master Dashboard `#hdr` block) was removed during the 2026-05-08 Shack-tab merge. OM polls already produce `type:'log'` messages that flow into the event log via Parse Open-Meteo output 2. No code change needed |
| 4 | ~~Format Log cosmetic~~ | **Done 2026-05-10** (`f15cad8`) — `(msg.distance != null) ?` instead of truthy ternary; 0 km strikes (AS3935 overhead/out-of-range) now render `\| 0 km` |
| 14 | ~~Power meter panels redesign~~ | **Done 2026-05-10** (`a2e6115`) — LP-700 + SPE WS Output Power bars: 10 px height (was 6 px), auto-scale 5/25/50/100/500/1000/1500/2000/5000 W, scale indicator next to header. LP-700: AVG=green, PEAK=amber, SWR keeps its threshold logic. SPE WS: removed redundant `0W / pwrMax W` text. Initial dynamic-threshold colour scheme on LP-700 power bars was reverted — auto-scale + dynamic % colours don't compose (bar always near top of its band → colour stuck at amber). |
| 15 | ~~DXCC alert-table dedup~~ | **Done 2026-05-10** (`8965d8e`) — `Format Alert for Dashboard Table` filter now drops existing rows matching `call+band+mode+alertType` before unshift. Combined with TTL expiry into a single pass. Identical spots re-firing after the 60 s upstream dedup window no longer double-render — the existing row gets replaced with the latest timestamp/freq/spotter |
| 5 | ~~Rotator timer 60s → 5min~~ | **Done 2026-05-10** (`971f4b4`) — `05f0ddeb566a90fc` body now `var duration = 5 * 60 * 1000` + status badge updated to "Timer running — 5 mins" |
| 6 | Mac SwiftUI app scaffold | Per CLAUDE.md TODO #12 — not started. Path now `~/projects/vu2cpl-shack-app/` (was `~/Documents/...`) |
| 7 | ~~Blitzortung real-time integration~~ | **Dropped 2026-05-10**, dead code stripped **2026-05-11** (`e8a2dd4`) — verified zero coverage at MK83TE on map.blitzortung.org. Sparse south-India contributor stations make TOA triangulation unreliable in this region. AS3935 (close-range) + Open-Meteo CAPE (regional) cover the operational need. Parser Cases 2/3 in `Parse Strike` (Buffer/string TCP payload parser, ~30 lines including `findKey` + `readCoord` helpers) deleted from the function body; CASE 1 (object payloads — test injects + Parse Open-Meteo) is the only path now |
| 8 | ~~DXCC backlog (pending #6–11 in CLAUDE.md)~~ | **Closed 2026-05-27 — audit confirms all sub-items done.** Per-item status: (a) **Filter persistence** — closed 2026-05-11 (HANDOVER #19, file-context store wiring + 5 reader functions aligned to 'file' scope). (b) **Separate CW/Ph/Data fetches** — `Fetch All Modes + Parse` fetches `mode0url..mode3url` (modes 0=All, 1=CW, 2=Phone, 3=Data) in parallel via `Promise.all`. Verified in flows.json. (c) **Non-project-folder path support** — `Bootstrap Worked Table` probes `flow.cfg_flows_dir + '/nr_dxcc_seed.json'` first, falls back to `os.homedir() + '/.node-red/nr_dxcc_seed.json'`. Either path works. (d) **README+PDF** — DXCC.md + DXCC_Tracker_README.pdf both last committed together on 2026-05-10 ([`cfea28f`](https://github.com/vu2cpl/2026-05-10-VU2OY-cluster-doc-fix)). In sync. Per CLAUDE.md rule #3 they regenerate together on every DXCC.md edit. (e) **Club Log API ban verification** — closed 2026-05-11 (HANDOVER #18, ban lifted, seed updates landing daily via 02:00 cron). (f) **Daily 02:00 inject wiring** — inject `c43fbdd61175ce24` "Daily club log refresh (0200)" wired with crontab `00 02 * * *`. Verified by node inventory. Net: HANDOVER row text was stale; every sub-item was de-facto closed weeks ago via the parent #18/#19 rows or the original 2026-05-10/11 work. |
| 9 | ~~Add `gpsntp` to RPi Fleet `httpDevices`~~ | **Done 2026-05-10** — `'gpsntp': 'http://gpsntp.local:7799'` added to `Route CMD: HTTP or MQTT` (`a0695975fec84e2c`). Reboot/shutdown buttons functional via the existing fleet-card mechanism |
| 10 | ~~Watch `gpsntp` through a U3S TX session~~ | **Closed 2026-05-11 (no issue)** — operator verified `gpsntp.local` holds stratum-1 PPS lock through **1 kW** SPE amplifier transmissions (well above the U3S WSPR power level that was the original concern). QLG1 patch antenna is not RFI-desensitised by the shack's high-power TX. Dedicated NEO-M8N fallback (documented in `pi-gps-ntp-server/HANDOVER.md`) is not needed |
| 11 | ~~Optional: chrony metrics on Shack dashboard~~ | **Done 2026-05-10** — Chrony status card landed via `743a0d8` + `286348e` migration to single `ui_template`; placement on `Shack Monitoring tools` → `Network Monitor` group |
| 12 | ~~Install `log2ram` on `gpsntp`~~ | **Done 2026-05-11** — azlux repo (`trixie main`, not `bookworm main` as BUILD.md said — gpsntp moved to Debian 13), `log2ram` installed + enabled, rebooted. `/var/log` now tmpfs 128 MB; chrony re-locked to stratum 1 with PPS within ~60 s of boot. BUILD.md upstream fix landed in [`pi-gps-ntp-server@5b115ba`](https://github.com/vu2cpl/pi-gps-ntp-server/commit/5b115ba) — release-agnostic `${VERSION_CODENAME}` + explicit reboot step |
| 13 | ~~Delete orphan `GPS NTP` flow tab~~ | **Done 2026-05-10** — operator deleted the empty tab; closed in next `nrsave` |
| 16 | ~~Clear LP-700-HID ws tab Description field~~ | **Done 2026-05-11** (`72fc31e`) — initially marked won't-do as cosmetic, but operator cleared it via editor → tab Properties → Description → empty → Deploy → commit. Tab sidebar is now blank (was 419 chars of legacy `npm install robertsLando/node-red-contrib-usbhid` + telepost udev notes from the pre-WS-gateway era) |
| 17 | ~~Fold DXCC extract regen into `nrsave` alias~~ | **Done 2026-05-11** — `nrsave` was an alias on `~/.bashrc:114`; converted to a bash function that runs the rule #4 extract regen before `git add`, then stages flows.json + the extract together. `~/.bashrc.bak.YYYYMMDD_HHMM` backup retained on Pi. CLAUDE.md rule #4 reworded to note nrsave handles it automatically; REBUILD_PI.md + rebuild_pi.sh updated so a fresh rebuild lands the same function definition (was carrying a stale `git save`-based variant) |
| 18 | ~~Club Log API ban verification (CLAUDE.md TODO #10)~~ | **Closed 2026-05-11** — ban lifted, confirmed by operator + verified live (`nr_dxcc_seed.json.updated` = 2026-05-11T03:27:47Z, written by the daily 02:00 cron). No flow change: `once: false` on the startup injects is the right defence — one fetch per day via cron, ad-hoc refreshes via `POST /dxcc/refresh`. CLAUDE.md TODO #10 row reworded, Data files list corrected (modes lives inside seed, maps comes from cty.xml runtime) |
| 19 | ~~DXCC filter persistence (CLAUDE.md TODO #6)~~ | **Closed 2026-05-11** — verified end-to-end. Two-part fix: (a) `enable_file_context.sh` had never actually run on this Pi — its idempotency check was matching the commented template `localfilesystem` mention in stock settings.js, and its substitution block was a single-store design that would have routed all no-scope traffic to disk. Rewrote to install two named stores (`memory` default + `file` localfilesystem), fixed the idempotency check (`^\s+contextStorage:` anchor), ran it on the Pi. (b) Aligned 5 reader function bodies (`DXCC Prefix Lookup + Alert Classify`, `Format Alert for Dashboard Table`, `Format FlexRadio Spot Command`, `Format Telegram Alert Dedup 10 minute`, `Format Telegram Alert`) to use `'file'` scope on their 32 filter*/spotTTL `flow.get` calls, matching the writer in `Save Alert Filters HTTP`. Tested: toggle a filter chip → restart Node-RED → fire matching TEST inject → no alert (suppressed as expected) → re-enable → fires |
| 20 | ~~DXCC worked-table dual-write bug~~ | **Misdiagnosis 2026-05-11 — closed without code change.** While investigating #19 I scanned for `'file'`-scope writes in `Fetch All Modes + Parse` and reported it as file-only. Wrong: the function does *triple* persistence — memory writes (L85-88, no scope), file context writes (L91-94, `'file'`), AND a direct `fs.writeFileSync` to `nr_dxcc_seed.json` (L96-103). Architecture has been correct all along; today's fix to enable the file context store just makes the L91-94 writes actually land where they always intended. Lesson: filtering tool searches by a single scope hides the parallel writes; always scan full function bodies when assessing dual-write integrity |
| 21 | ~~AS3935 outdoor sensor — ESP32 firmware project~~ | **v0.2.0 live on the bench 2026-05-12** ([`vu2cpl-as3935-bridge`](https://github.com/vu2cpl/vu2cpl-as3935-bridge)). v0.1.1 bench bring-up done 2026-05-11; v0.2.0 same day added the full MQTT cmd channel (subscribes `lightning/as3935/cmd`, acks on `cmd/ack`), NVS persistence of all tunables, range validation, on-device TUN_CAP sweep as the `calibrate_tun_cap` action (port of `as3935_tune.py` w/ MQTT keepalive pumped inside the 35 s loop), and a no-publish watchdog → `ESP.restart()`. Still open for v0.3.0+: sleep-on-IRQ for battery mode + OTA. Hardware-outdoors install (HANDOVER #1) still pending — enclosure / power chain / field TUN_CAP retune |
| 22 | ~~Distance-graded disconnect~~ | **Done 2026-05-12** ([`76d60e5`](https://github.com/vu2cpl/vu2cpl-shack/commit/76d60e5)) — 3×3 matrix (AS3935 close/medium/far × OM cold/lit/severe) implemented in `Trigger Disconnect`. OM no longer directly fires DC; serves only as corroboration. 7 cfg keys live-tunable from `Init Defaults`. Matrix + thresholds documented in CLAUDE.md "Lightning Antenna Protector" section. Net behaviour shift: fewer false-positive DCs during high-CAPE-no-storm Bengaluru afternoons; close-zone single-hit still fires (deliberate, safety-first) |
| 23 | ~~AS3935 Control Panel ui_template~~ | **Done 2026-05-12** — new self-contained admin panel on flow tab `AS3935 Tuning` (`fe70cfdcdfa19aa4`). Exposes all v0.2.0 firmware tunables + actions. Pattern-B IIFE `(function(scope){…})(scope)`. Survived a scope-binding refactor bug ([`7d205c1`](https://github.com/vu2cpl/vu2cpl-shack/commit/7d205c1)) and a rollback-tab pattern experiment (kept old tab disabled for 1 day as insurance, then deleted). GitHub-dark palette matching chrony card |
| 24 | ~~Audit Node-RED flow for unintended `power_on` over spe-remote WS~~ | **Closed 2026-05-27 — audit CLEAN, no Pi-side issue.** Full audit of `spe_ws_tab_01`: 0 inject nodes, 0 link-in, 0 mqtt-in, 0 http-in. Only `ws_out_node` (websocket-out) is the path to spe-remote, and only `ws_btn_router` (function) wires into it. `ws_btn_router` has exactly two upstreams — `ws_panel_node` (D1 SPE Panel: `window.speAmpToggle()` with `if(!confirm(prompt)) return;`) and `vue_spe_cmd_router_01` (Vue SPECard's `togglePower()` also gated by confirm dialog). **No node in flows.json emits a literal `payload:'power_on'`/`'power_off'`** outside the `ws_btn_router.map` constant, and that map is only reached on alphabetic `'ON_SPE'`/`'OFF_SPE'` payload which only flows from human button presses. No echo loop on the SPE tab either — `ws_in_node` traffic flows through `ws_format_state`/`ws_format_hb`/`ws_format_pwr_res` which all terminate at the panel/Vue bridge, none wire back to `ws_out_node`. Conclusion: MacExpert STBY-banner flicker is NOT caused by Node-RED stray injects; the Mac-side strict-decoder fix (spe-remote/handover.md item 1) is the only fix path. Pi-side clean. |
| 25 | ~~High CAPE is logged as a "0 km strike"~~ | **Closed 2026-05-27** — Option (a) implemented. `Parse Open-Meteo → Strike` (`593f22a507b46335`) no longer emits a strike-shaped payload at all; only emits `type:'cape'` + `type:'log'` messages to Master Dashboard. Outputs 2→1, wire to Parse Strike removed. Matrix corroboration unaffected (still reads `flow.om_state` set on every poll). Log line wording rewritten to describe the OM signal without faking distance: "Open-Meteo: Severe CAPE 2800 J/kg → SEVERE @ 14:00" (was "Open-Meteo: TS NOW → 0 km @ ..."). CLAUDE.md "Lightning Antenna Protector" + "Key Node IDs" tables updated. Note that the JSONL event log + Vue dashboard event log will no longer show OM-only events as strike entries — they only appear as log lines now, which is the intended semantics ("the sky is bad" ≠ "lightning happened here"). |
| 26 | ~~D1 `/ui` backport — AS3935 Bridge Tuning + Lightning merge~~ | **Closed 2026-05-27** — Done as part of the Bridge-Tuning + Master-Dashboard merge ([`38bf3fb`](https://github.com/vu2cpl/vu2cpl-shack/commit/38bf3fb)). AS3935 Control Panel (223cb2ce733c5d3f, including its 2026-05-17 Events sub-section) absorbed into Lightning Master Dashboard (557083037f168b22) as a `<details>` collapsible right after the existing as3935Card mini-display. The standalone ui_template + its dashboard group deleted; height bumped 12 → 22 to fit. 7 incoming wires re-pointed; AS3935 cmd mqtt-out added to Master Dashboard output fan-out. D1 + Vue /shack now match architecturally. |
| 27 | ~~D1 `/ui` backport — DXCC per-cluster click-to-mute pill~~ | **Closed 2026-05-27** ([`0153c9a`](https://github.com/vu2cpl/vu2cpl-shack/commit/0153c9a)) — Each cluster node (VU2CPL/VU2OY/VE7CC/N2WQ) on the DXCC Dashboard now has onclick handler firing scope.send with topic=`dxcc_mute`. New dxcc_mute_handler_01 function downstream updates `global.dxcc_cluster_mute` — same global honoured by login-parse-dedup-v2 so D1 + Vue share mute state. Visual: muted clusters render at 0.4 opacity with "MUTED" label (amber). Cluster Watchdog now attaches `payload._muted` to cluster_status so dashboard rehydrates correctly on page open. |
| 28 | ~~D1 `/ui` backport — SPE auto-ranging power bar ladder~~ | **Closed 2026-05-27** ([`b9b727c`](https://github.com/vu2cpl/vu2cpl-shack/commit/b9b727c)) — Added 10 W and 250 W rungs to D1 SPE Panel's SPE_SCALE constant. Was `[5, 25, 50, 100, 500, 1000, 1500, 2000, 5000]`; now `[5, 10, 25, 50, 100, 250, 500, 1000, 1500, 2000, 5000]` matching Vue PWR_LADDER. Low-power tune carriers (1–10 W) now read meaningfully on D1. |
| 29 | ~~D1 `/ui` backport — Lightning card stats-grid trim~~ | **Closed 2026-05-27** ([`38bf3fb`](https://github.com/vu2cpl/vu2cpl-shack/commit/38bf3fb)) — Done alongside the AS3935 merge. Dropped Callsign + Grid Square rows from Master Dashboard's stats rendering (already shown in Header card). Kept Threshold + Reconnect + Antenna — useful at-a-glance and Vue keeps them too in its Thresholds collapsible. |
| 30 | ~~D1 `/ui` backport — DXCC seed-age display~~ | **Closed 2026-05-27** ([`0153c9a`](https://github.com/vu2cpl/vu2cpl-shack/commit/0153c9a)) — New `Seed age: 3h ago` row in DXCC Dashboard's WORKED STATS card. Backed by dxcc_seed_age_fn_01 (fs.statSync on nr_dxcc_seed.json, libs:[fs,os]) on a 30s tick inject. Same data + same relative-time formatter as vue_dxcc_builder_01 uses for the /shack DXCC card. Amber colour for >30d ages. |
| 32 | ~~Auth for external dashboard access (both `/ui` and `/shack`)~~ | **Simplified + closed 2026-05-27.** Original sketch called for Caddy + Let's Encrypt + LAN-bypass — over-engineered. Operator clarified: "username/password always" is fine. So implementation collapsed to a 10-min `settings.js` edit reusing the existing `adminAuth` bcrypt hash. `httpNodeAuth` block uncommented + populated with the `vu2cpl` credentials; `ui:` block uncommented + given a matching `auth.users` array. All three contexts (editor / D1 `/ui` / Vue `/shack` / HTTP endpoints `/lightning/*` `/dxcc/*` `/rotor/*`) now require login. uibuilder is served under Node-RED's HTTP layer so `httpNodeAuth` covers it without needing `useSecurity` to be set on the uibuilder node. Single credential (Safari remembers it). Backup at `~/.node-red/settings.js.bak.20260527_222201` on Pi if rollback ever needed. **Upgrade path for off-LAN access** (deferred — not actually needed today): add Caddy + Let's Encrypt on top for TLS termination + router port-forward 443. The auth gate stays — Caddy just adds encryption-in-transit. The simpler base is strictly upgrade-compatible with that approach. |
| 33 | ~~**Manual disconnect = sticky-off (no auto-reconnect); auto-strikes during this state are alert-only**~~ | **Closed 2026-06-03** ([`82c85ea`](https://github.com/vu2cpl/vu2cpl-shack/commit/82c85ea)). Operator's bug report: clicking Manual Disconnect cancels the reconnect timer correctly, but any auto-strike during the manual-off state re-arms the timer behind the operator's back → 20 min later antenna auto-reconnects mid-storm. Fix: 6 surgical edits, no new nodes, no new endpoints, no dashboard buttons. (a) `Manual Override` writes `flow.manual_off` in file-context (survives Node-RED restart). (b) `Trigger Disconnect` early-exits when `manual_off === true` and emits an `auto_strike_while_manual_off` event_record on a new output 2 (wired to `light_jsonl_append_01` → `tg_lightning_router`); outputs 2→3. (c) `Force Reconnect` / `HTTP → Antenna ON` / `Execute Reconnect` all clear `manual_off`; Execute Reconnect coverage means Bypass-ON auto-clears manual hold (Bypass Handler output 3 wires there). (d) `Telegram Alert Router` allow-list adds the new event; formatter adds 🟣 MANUAL HOLD case. Per HANDOVER #19 (DXCC filter persistence) scope-alignment lesson — all `manual_off` reads/writes use the explicit `'file'` scope. Test plan documented in SHACK_CHANGELOG `2026-06-03` entry. Original TODO text preserved below for archeology. ── ORIGINAL: **Opened 2026-06-01.** Operator scenario: user sees a storm approaching (radar / visual / thunder), manually clicks Disconnect on the dashboard, walks away. **Today's bug:** manual disconnect goes through the same `Trigger Disconnect` path as an auto-strike, which fires the 20-min reconnect timer → 20 min later the antenna auto-reconnects mid-storm. Undesirable — the operator chose OFF, the radio should stay off until *they* say otherwise. **Desired behaviour:** manual DC is a "sticky" state — `flow.manual_off = true`, no reconnect timer started, antenna stays OFF + TX inhibited until the operator clicks Reconnect (clears the sticky flag). Subsequent auto-strikes while `manual_off` is true do **alert-only with different text** (different from both the first auto-DC alert and the auto-DC "STORM CONTINUES" alert): "Strike at X km · antenna already OFF (manual) · no action taken". No MQTT OFF re-send, no timer manipulation, no inhibit re-fire. **Distinction from existing Bypass switch:** Bypass-ON = "ignore lightning automation entirely, keep antenna ON" (operator-override-to-transmit). This is the opposite — "operator override to stay OFF". Different flag, different semantic. **Implementation sketch:** (a) Add a `Manual Disconnect` button on the dashboard that fires a new HTTP endpoint `/lightning/manual-off`. The http-in's function sets `flow.manual_off = true`, calls into `Tasmota Antenna Switch` + `FlexRadio TX Inhibit`, but does NOT start the reconnect timer. Emits an `event_record` so JSONL + Telegram get a `manual_off` event (new type, allow-list update in `tg_lightning_router`). (b) Add a `Manual Reconnect` button → `/lightning/manual-on` → clears `flow.manual_off`, fires Tasmota ON + TX uninhibit, emits `manual_on` event. (c) `Trigger Disconnect` — second early-branch (after the bypass guard): if `flow.manual_off === true`, emit an `event_record` with new type `auto_strike_while_manual_off` and return — no MQTT, no timer, no inhibit. The matrix still runs to *classify* whether the strike would have DC'd, so the alert text can say "matrix would have DC'd: yes/no, X km, OM=lit". (d) `Execute Reconnect` (timer expiry) — guard so even if a stale timer fires somehow, it no-ops when `manual_off` is true. (e) Dashboard state badge — surface `manual_off` distinctly from `antenna_off` (e.g., antenna pill turns purple for manual-off vs amber for auto-off). (f) Telegram allow-list adds `manual_off`, `manual_on`, `auto_strike_while_manual_off`. (g) Persistence — `flow.manual_off` survives Node-RED restart via the `file` context store (same pattern as DXCC filter persistence, HANDOVER #19) so a restart doesn't silently re-enable the antenna. Worth the few extra lines. **Not blocking but real safety item; capture before next storm season.** |
| 31 | ~~**Rotator → WebSocket gateway**~~ (same pattern as SPE + LP-700) | **Closed 2026-06-06.** Built [`vu2cpl/rotator-remote`](https://github.com/vu2cpl/rotator-remote) (Python/Tornado, `:8090`) — single FTDI-serial owner, multi-client fan-out, mirroring spe-remote (thread reader + asyncio writers, one write-lock, dedup+heartbeat broadcast). Protocol bytes extracted **verbatim** from the old flow (`AI1;` query · `AP1NNN\r` set, CR terminator · `;` stop · `;`-framed replies) and bench-verified against the real rotor — goto turned it clockwise through north (358→5 wrap handled), stop/lpsp/multi-client all confirmed. Node-RED Rotator tab refactored to a ws-client (flows.json [`626ccc0`](https://github.com/vu2cpl/vu2cpl-shack/commit/626ccc0), 501→497 nodes): deleted 8 serial-path nodes, added the ws-client trio + `Parse Rotator WS`, retargeted `Send Rotator → WS` to translate the `AP1NNN\r`/`;` strings that `Build Rotator String` + `Click → Heading` still emit into `{type:command,action:goto\|stop,heading}` JSON (their heading logic untouched). **Power stayed on Tasmota/MQTT** — serial/heading only, per the operator's scope decision. `rebuild_pi.sh` Stage 13b installs the gateway (opt-in, with serial-port prompt + `/healthz` check). Decisions taken in plan mode: new standalone repo (vs in-shack scripts) + serial-only (vs gateway-owns-power). Original spec preserved below. ── ORIGINAL: **Opened 2026-05-27.** Currently the Rotator flow tab holds the Rotor-EZ FTDI serial port directly (`/dev/serial/by-id/usb-FTDI_FT232R_USB_UART_AL05J29R-if00-port0`, 4800-8N1, Rotor-EZ protocol). Single-owner — Node-RED grabs the port at deploy and nothing else on the Pi can talk to the rotator while it's running. **Goal:** lift the serial port out of Node-RED into a small Python (or Go) WebSocket gateway service running as systemd, mirroring the existing pattern for SPE (`spe-remote.service`, owns SPE FTDI) and LP-700 ([`VU3ESV/LP-700-Server`](https://github.com/VU3ESV/LP-700-Server) → `lp700-server.service` on port 8089, owns LP-700 HID). After conversion the Node-RED Rotator tab becomes a thin WebSocket client; the Mac SwiftUI app (HANDOVER #6) and any third client could subscribe in parallel without serial-port contention. **Suggested scope:** (a) new repo `vu2cpl/rotator-remote` (or add to `vu2cpl-shack`'s Pi-side scripts folder) — Python websockets + pyserial owning the FTDI port, accepts JSON commands like `{cmd:'goto', heading:120}` / `{cmd:'stop'}` / `{cmd:'long_path'}`, broadcasts state JSON `{heading_now, target, moving, lp_sp, ts}` to all clients ~5 Hz; (b) systemd unit `rotator-remote.service` with udev rule for hidraw-style permissions; (c) Node-RED Rotator tab refactored: delete the existing serial-in/serial-out nodes, replace with `websocket-client` to `ws://localhost:<port>/ws`, keep `Build Rotator String` function adapted to emit JSON commands, keep dashboard panels intact. (d) Vue RotatorCard either stays via Node-RED bridge (no change) or talks to the gateway directly (cleaner — drops one hop). Template files to copy from: SPE-side `~/projects/spe-remote/` (Python websockets) is the closest analogue; the Rotor-EZ protocol is much simpler than SPE's 76-byte framed binary, so the gateway should be ~300 lines. **Benefits captured in REBUILD_PI.md / FORK_GUIDE.md once shipped:** unified architecture across all three USB devices, no more "restart Node-RED to free the rotator port", multi-client (browser dashboard + MacExpert-equivalent + Mac SwiftUI app + future), cleaner failure isolation. Medium-large effort — new service + tests + deploy + udev + flow refactor. Not blocking. |
| 34 | ~~**SPE Tune — proper button + indicator reflecting the amp's front-panel TUNE LED**~~ | **Closed 2026-06-19 — confirmed working on-amp.** Operator ran a real TUNE cycle; the Tune button goes amber on both UIs while the front-panel LED is lit, clears when done. (Plus a `v14` copy fix to the Vue confirm dialog — "Transmit a low-power tuning carrier within a few seconds to start tuning", since the amp doesn't key itself, the operator does.) ── Implementation: **Opened + implemented 2026-06-19.** Gateway already shipped the LED bit (spe-remote `serial_handler.py:639` decodes byte 4 bit 6 of the RCU frame → `state.tune_active`, RCU auto-enabled), so this was Node-RED + Vue only: `ws_format_state` now emits `tune: !!d.tune_active`; the **Tune button's fill colour toggles** to mirror the amp LED — D1 `#spew-tune-btn` neutral→amber (`background:var(--gh-amber)`+glow+`●`), Vue button `btn--blue`↔`btn--amber`+glow+`●`, plus a Vue `⚡ TUNE` collapsed-header chip (`tune:false` in state, build **v13**, `?v=13`). **First cut (v12) left the button permanently amber with only a box-shadow toggle → read as "no indication"; revised same day to a real fill-colour change.** `node --check` + flows.json round-trip clean; WS probe confirmed the gateway sends `tune_active=false` in Standby. **Still open until a real TUNE cycle confirms the button goes amber on both UIs and clears when done.** See SHACK_CHANGELOG 2026-06-19 (SPE Tune). ── ORIGINAL: **Opened 2026-06-19.** Two parts, on **both** dashboards: D1 `/ui` SPE Panel (`ws_panel_node`, `20fcd2e...` is the CSS) and Vue `/shack` SPECard (`confirmTune()` @ [`uibuilder/shack/src/index.js:1525`](uibuilder/shack/src/index.js#L1525)). (1) **Button:** Vue already has an amber `Tune` button (`confirmTune()` → spe-remote WS, `:disabled="!powerOn"`) plus the SWEEP/ATU-band panel from the recent band-sweep work; D1 `ws_panel_node` has tune/ATU refs (`tune`×7, `atu`×15) — verify/bring it to parity with a real momentary Tune button. (2) **TUNE-LED indication (the real new ask):** surface the SPE's front-panel **TUNE LED** state on the dashboard so the operator can see whether the ATU is *currently tuning / tuned* — the button should glow / show a ● LED that mirrors the amp. Spans 3 layers: **(a)** locate the TUNE-LED bit/byte in the 76-byte SPE poll frame in the `vu2cpl/spe-remote` Pi service (`spe/` parser) and add it to the broadcast state JSON; **(b)** thread it through `ws_format_state` in flows.json (D1) and the Vue SPECard reactive state; **(c)** render the indicator on both UIs. Anchor fields already present in flows.json: `atuswr`, `speTuneBand`, `speTuneStop`, + ATU/TUNE refs in both panels — check whether any already carries the LED bit before adding a new one. Not blocking. |

---

## Key files & IDs to know

```
flows.json                        — main flow file (Pi: ~/.node-red/projects/vu2cpl-shack/)
as3935_mqtt.py                    — chip daemon (Pi: /home/vu2cpl/, source-of-truth in repo root)
as3935_tune.py                    — tuning helper (run with service stopped)
SHACK_CHANGELOG.md / .pdf         — must stay in sync (regen PDF on every changelog edit)
nr_dxcc_seed.json                 — auto-updated by Club Log fetch; commit when changed
HANDOVER.md                       — this file
```

Critical Node-RED IDs (per CLAUDE.md):

- `75e2cac8ab96f556` Lightning Antenna Protector tab
- `557083037f168b22` Master Dashboard
- `26ddff0cbbfe5fc1` Parse Strike (handles object payloads from OM + test injects; Blitzortung Buffer/string parser stripped 2026-05-11 `e8a2dd4`)
- `d62fb0c3c40f03b7` Trigger Disconnect
- `593f22a507b46335` Parse Open-Meteo → Strike (CAPE-based)
- `61dca3d98a0e4c28` Refresh Stats — fan-out hub for all 30 s replays
- `f4785be9863eab08` MQTT broker config (192.168.1.169:1883, no auth)

---

## Workflow reminders

- **`cdp`** = Commit, Document (changelog + regen PDF), Push. Updates DXCC extract too if `flows.json` changed.
- **Mac → Pi** for script changes: edit on Mac, `git push`; on Pi: `git pull` then `sudo cp <file> /home/vu2cpl/<file>; sudo systemctl restart <svc>`.
- **Pi → Mac** for flow changes: `nrsave "<msg>"` on Pi; `git push`; on Mac: `git pull`.
- **PDF regen:** `npx --yes md-to-pdf SHACK_CHANGELOG.md` (no pandoc on this Mac).
- **AS3935 systemd unit needs** `Environment=PYTHONUNBUFFERED=1` in override or prints don't reach journalctl.

---

## Known weirdness

- `~/Documents` on this Mac is **local**, not iCloud-synced. Only `~/Desktop` symlinks to iCloud Drive. The 05-07 folder consolidation was for organizational tidiness only.
- `/home/vu2cpl/as3935_mqtt.py` had become **owned by root** during earlier sudo edits — `cp` without sudo silently no-op'd. Always check ownership after first deploy. Fix: `sudo cp + sudo chown vu2cpl:vu2cpl`.
- AS3935 with NF=0 fires a noise burst at startup (~209 events in 30 s) then the chip's auto-noise-track suppresses further until conditions actually change. NF=4 is the production default; NF=0 only for diagnostics.
- **DXCC blacklist has no off-Pi backup since 2026-05-13** ([`b1fbef9`](https://github.com/vu2cpl/vu2cpl-shack/commit/b1fbef9) public-prep). `nr_dxcc_blacklist.json` is `.gitignore`d to avoid publishing the muted-callsigns list. Deliberate trade-off — accept that a Pi disk failure means the list evaporates and gets repopulated from memory. List is small (<20 entries typically), recoverable. If it ever grows large enough to be painful to lose, set up a periodic rsync to the Mac. Same `.gitignore` reasoning applies to `nr_dxcc_seed.json` and `nr_lightning_events.jsonl`, but those are runtime caches — Club Log / AS3935 events recreate them naturally, no loss.

---

## Recent commit log (for context)

```
a5a358f As3935 banner behaviour change when strike within38km
2779b04 Lightning: tier-3 cleanup — rename, demote warn, merge dashboard handlers
2cacc7a SHACK_CHANGELOG + HANDOVER: 2026-05-10 — Lightning tier-2 cleanup
6578c79 Lightning: tier-2 cleanup — drop Check Threshold + Refresh Stats passthroughs
de7c323 SHACK_CHANGELOG + HANDOVER: 2026-05-10 — Lightning tier-1 cleanup
f8ee18c Lightning: tier-1 cleanup — drop dead nodes, wires, vars
30d540e DXCC seed refresh
4791c2f some minor cleanup of unused nodes and deleted ols spe serial flow
e9c7d8e SHACK_CHANGELOG + HANDOVER: 2026-05-09 — Pi GPS NTP server build + gpsntp on RPi Fleet
0c1546d Add DEPLOY_PI.md — full per-Pi onboarding runbook
964cb04 Pi-side scripts: power_spe_on.py in, fetch_clublog.sh retired
8636ff4 Pi-side scripts: check in rpi_agent.py + monitor.sh + rpi-agent.service
9bb9631 Add MIT LICENSE + README badge
bb7415b SHACK_CHANGELOG: 2026-05-09 footnote — LP State Aggregator payload-shape fix
08c907f LP-700: fix LP State Aggregator to read msg.payload (new Reshape shape)
28e8426 SHACK_CHANGELOG + HANDOVER + CLAUDE.md: 2026-05-09 — LP-700 → WebSocket gateway
c449c49 LP-700: switch from direct HID to lp700-server WebSocket gateway
b3261ba SHACK_CHANGELOG + HANDOVER: 2026-05-08 — Lightning UI on Shack tab + bypass + last-activity recap
6a03506 Lightning: integrate to Shack tab + bypass + last-activity recap
6ac6fef DXCC extract refresh
0021bcd DXCC extract refresh
167133e Lightning: integrate to Shack tab + bypass switch (WIP — bypass not suppressing disconnect)
45bd8dd SHACK_CHANGELOG + HANDOVER: 2026-05-08 — Lightning map ripped out, gauge persists across refreshes
bf1b941 Lightning: rip out leaflet map; persist nearest-strike gauge across refreshes
46a96e8 Add HANDOVER.md — session pickup notes for 05-01 to 05-07
497ccf3 CLAUDE.md: consolidate vu2cpl repos under ~/projects/
08647ce Dashboard: AS3935 panel shows live ready/heartbeat state; Event Log moved above Map
c2c2375 SHACK_CHANGELOG: 2026-05-06 — AS3935 dashboard liveness panel
08b8511 SHACK_CHANGELOG: 2026-05-06 — AS3935 GPIO4 fix, LC tank tuning, CALIB_RCO
996c211 AS3935: re-publish status after calibration so calib_trco/srco aren't null
26c4cd3 AS3935: add CALIB_RCO at startup + INT flush + calib result in status
e20385f AS3935: apply TUN_CAP=10 (tuned to 499.9 kHz, -0.02% err); publish tun_cap+irq_pin in status
c764bda AS3935: IRQ pin is GPIO4, not GPIO17 (verified by SRCO scan)
97fbc4a AS3935: add LC tank tuning helper (TUN_CAP sweep 0..15)
acad3c5 SHACK_CHANGELOG: 2026-05-01 — AS3935 revival, Open-Meteo CAPE, source label, dashboard refresh persistence
2d69530 Lightning: persist strikes + log to flow context, replay on 30s tick
114d8a4 Lightning: propagate source through Parse Strike (Open-Meteo label fix)
b92bfb9 Open-Meteo: switch from null lightning_potential to CAPE + weather_code
```

---

*73 de VU2CPL*
